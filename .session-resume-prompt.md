# Session 44 Cold-Start Prompt(2026-07-09 以降、session 43 全 chain CLOSED)

**このプロンプトは別セッションでの cold-start に必要な情報がすべて含まれています。
上から順に読んで作業再開できるよう構成されています。**

---

## ★★★ STEP 0:セッション開始直後、必ず実行

```bash
cd /Users/tokuyama/workspace/clinosim
git branch --show-current
git log --oneline -1 master
git log --oneline -1 origin/master
git status --short
git worktree list
```

**期待値**:
- current branch: `master`
- master HEAD = origin/master HEAD = `2407d75f85`(session 43 wrap、YJ chain closure)
- working tree clean、未 commit なし
- worktree は本体 1 つのみ

master でない場合はまず `git switch master`。git status で differences がある場合は前セッションの残
処理を確認(memory/feedback_session_start_branch_check.md 参照)。

---

## ★★★ STEP 1:プロジェクトコンセプトと設計を把握してから作業

**cold-start 時は memory や本 prompt の記憶に頼らず、プロジェクトそのものの構造を実際に読んで
把握してから作業する**(session 33 で確立、以後全 session 遵守)。最低限、以下の順で必読:

### 必読 docs(順序重要)

1. **`docs/design-guides/project-concept-and-design.md`** — ★ プロジェクトコンセプトの 9 要求
   + パイプライン全体像。「なぜ何を作っているか」の土台
2. **`docs/design-guides/implementation-rules.md`** — ★ 実装不変則の蒸留版
   (AD-16 決定性、AD-30 CIF code-only、AD-55/56 モジュール境界、§6.5 共通ロジック所在マップ)
3. **`MODULES.md`** — 30 モジュールの一覧(依存関係含む)
4. **`CLAUDE.md`**(project root)— コード規約・アーキテクチャルール・言語規約

### 直前サイクル記録(session 42 の完全記録)

5. **`docs/audit-cycles/cycle-3.md`** — cycle 3 完全記録(30 issues + fix + verify + review 反映)
6. **`docs/audit-cycles/cycle-2.md`** — cycle 2 完全記録
7. **`docs/audit-cycles/README.md`** — 監査サイクルワークフロー(step 8 の end-of-cycle
   fix review、step 11 の 勝手な carry-over 禁止 は session 42 で確立)

### コード構造理解のための sub-agent 呼び出し(オプション)

大きな新規機能着手前は `general-purpose` agent や `Explore` agent を使って以下を並列調査:
- `clinosim/modules/` 30 モジュールのうち作業対象周辺
- 直前サイクルの実装例(`git log --oneline -20 master`)

### subagent へタスク委譲時の必須事項(session 33 教訓)

Agent tool / subagent-driven-development でタスクを渡す時、**dispatch prompt 本文に毎回明示**:
1. `docs/design-guides/implementation-rules.md` を実装前に読むこと
2. そのタスクが模倣すべき既存 precedent ファイルの具体的 path
3. 再実装禁止の canonical single-source helper 一覧(`_coding_with_display` / `to_fhir_datetime`
   / `to_fhir_date` / `_apply_jp_core_profile` / `_mr_intent_from_order` / `_o` /
   `set_attr_or_key` / `get_or_create_container` / `code_lookup` / `is_jp` / `is_us` /
   `resolve_lang` / `load_code_mapping` / `system_key_for` 等)
4. そのタスクに関連する具体的 ADR 番号
5. worktree 作業時は「絶対パスを全 Read/Edit/Write で使うこと」+「worktree 外への stray commit
   注意」を明示(memory `feedback_subagent_worktree_and_background_task_pitfalls.md`)

---

## ★★★ STEP 2:現状把握(session 43 完全終了状態、7 chain CLOSED)

### Master HEAD

`2407d75f85`(session 43 wrap = YJ chain closure、origin/master 完全同期)

### Session 43 の commit history(古い順)

1. `242bfe061f` feat(cycle-4): 22 fixed / 3 partial / 5 deferred
2. `903ff4362f` feat(cycle-5): 19 fixes / 10 defer to structural work
3. `6bc0d196d4` fix(c1-c4-residual): C2-19 / C3-03 / CY2-B carry-overs (3)
4. `78f89bd350` feat(imaging-chain): CO-1 body_sites 拡張 (1)
5. `c5213a4661` feat(history-chain): C5-22 classHistory / statusHistory (1)
6. `ca051e0980` feat(secondary-dx): C5-07 / C5-12 (2)
7. `2407d75f85` feat(yj-chain): CO-8 authoritative subset (1)

### Session 43 総成果: **49 issues resolved**

| Phase | 件数 |
|---|---:|
| Cycle 4 | 22 |
| Cycle 5 | 19 |
| C1-C4 residual (C2-19 Kana / C3-03 lot / CY2-B compound) | 3 |
| Imaging chain (CO-1、10 body sites × 4 modalities) | 1 |
| History chain (C5-22 classHistory / statusHistory) | 1 |
| Secondary dx + orphan MR (C5-07, C5-12) | 2 |
| YJ chain (CO-8 partial、27 authoritative MHLW 薬効分類 codes) | 1 |

**Session 42 の commit history(古い順)**:
1. `61a90fcfe0` feat(cycle-3): CLOSE cycle 3 — 20 fixed / 4 partial / 6 attempted-defer
2. `6435ce05af` review(cycle-3): revert fabricated codes/stubs per user directive
3. `77b8d9a362` feat(c2c3-tail): resolve remaining cycle-2/3 open items (RM-1..8)
4. `243e8f7b68` fix(RM-3): move roster pass to POST_RECORDS stage
5. `d5a484e701` feat(RM-6/7-full): complete resolution of MR classification + JP chronic gap
6. `1f3f5b0fa8` docs: session 42 wrap — C2/C3 tail + RM-6/7 full closure recorded

### 主要 metric(JP p=10000 seed=42 実測、session 42 終了時点)

| 指標 | Baseline(cycle 3 開始時) | 現状(session 42 終了時) | 評価 |
|---|---:|---:|---|
| Procedure count | 361 | **2,236 (6.2x)** | ✅ RM-6 |
| JP problem-list-item /enc | 1.20 | **4.92** | ✅ RM-7 US 2.49 超過 |
| meta.profile 100% 対象 | 4 | **13/13** | ✅ Procedure 追加完備 |
| Observation.performer coverage | 43% missing | vital-signs **0.0%** / survey **0.2%** / laboratory 0.7% | ✅ RM-1 |
| MR.intent 多様化 | all order | order 4998 + **instance-order 1465** | ✅ RM-2 |
| MR.status 多様化 | active + cancelled | active 4624 + **completed 1835** + stopped/cancelled | ✅ RM-2 |
| Immunization.performer | 0/25437 | **25,437/25,437** (100%) | ✅ RM-3 |
| GOLD 1/2/3 SNOMED | text only | 465 + 718 + 424 | ✅ RM-4 |
| ImagingStudy count | 103 | **208 (2x)** | ✅ RM-5 |
| YJ code fabrication | 68 codes | **0**(削除、text-only fallback) | ✅ RM-8 |
| Test | unit **2,338 全 PASS** | | ✅ |

### Session 42 主要成果物

**新規 codes/data YAML**(全て jpfhir.jp / SNOMED CT / LOINC 権威検証済):
- `hl7-condition-clinical.yaml` / `hl7-condition-ver-status.yaml`
- `hl7-v3-administrativegender.yaml` / `hl7-subscriber-relationship.yaml`
- `hl7-practitioner-role.yaml` / `hl7-v3-actreason.yaml`
- snomed-ct.yaml 拡張:185349003/11429006/394914008 + 313296004/313297008/313299006(GOLD 1/2/3)
- loinc.yaml 拡張:90557-9 NEWS2

**Canonical helpers**:
- `_coding_with_display(system_key, code, lang)` in `_fhir_common`
  = display-fallback 防止 single edit point
- `_apply_jp_core_profile(resource)` + `_JP_CORE_PROFILES` dict in `fhir_r4_adapter`
  = **14 JP Core URL、全て jpfhir.jp WebFetch 権威検証済**(cycle-3-tail で JP_Procedure 追加)
- `to_fhir_datetime` / `to_fhir_date`(FP-UNIFY-2、session 40)
- `_default_coverage_period_year`(Coverage.period fiscal-year 4/1-3/31)
- `_mr_intent_from_order(order, encounter_type)`(MR.intent 多様化 heuristic)

**Simulator 主要変更**:
- JP demographics.yaml:comorbidity_correlations tune-up + 7 chronic 追加(E79/H26/K59/I84/K74/M54/F32)
  + age-stratified band + care_seeking block
- inpatient.py:acquired chronic conditions 追加(disease_id → implied ICD codes 表、RM-7d)
- order/engine.py:supportive device_procedure_kw 分類(RM-6b)
- fhir_r4_adapter.py:PROCEDURE-Order → FHIR Procedure builder(RM-6c)
- immunization/engine.py + enricher.py:roster nurse pool 経由 administered_by(RM-3)
- types/encounter.py:ImmunizationRecord に lot_number + administered_by field
- code_mapping_drug.yaml:68 fabricated code 削除(RM-8)

---

## ★★★ STEP 3:Session 44 opening 候補(session 43 で defer した項目)

### 高優先度(structural work / authoritative source 要)

1. **Classification refinement** — MR codeless 17.6% 残の 60% は nursing/device の MEDICATION
   誤分類(経口水分摂取励行 / 暗室安静 / 経過観察 / ネブライザー / 心電図モニタリング / 非固着性
   ドレッシング 等)。CIF Order → THERAPY/PROCEDURE dispatch systematic fix(YAML treatment[]
   name → order_type ルール表を order/engine.py に追加)
2. **CO-6 GOLD 4 / HTN Stage / CCS SNOMED** — tx.fhir.org 未接続、authoritative SNOMED CT
   International browser 別 access ルート探索(SNOMED CT UMLS API / Snowstorm 復活 等)
3. **CO-8 remainder** — 12-char 個別 YJ codes(MHLW 令和6年 薬価基準 CSV parse chain)
4. **C5-19 CIF order compound multi-alt full split** — 現在 first alternative のみ選択、
   full split で separate Order 生成(2/3 alternatives の統計的 pick 追加)
5. **C5-20 DR.presentedForm patient-facing PDF/HTML** — 患者向け summary attachment
6. **C5-25 roster expansion** — 93 practitioners → hospital scale 拡大(config change)

### 中優先度

7. **Drug name normalization** — スライディングスケールインスリン / 生理食塩液変種 の canonical
   key normalization
8. **Session 42 memo にあった小 items** — 全 defer 済み

### 旧 (session 42 → cycle 4/5 で解消済み)

1. **RM-7 過剰性検証** — JP problem-list-item /enc = 4.92 は US 2.49 の 2x
   - EHR realism 観点で high の可能性
   - elderly cluster tuning(age-stratified prevalence を若干下げる or comorbidity multiplier
     を conservative 側に戻す)
   - Cycle 4 で「実 EHR の chronic 発火率 vs 現状」比較を含めた review 必要

2. **inpatient daily-loop 残 procedure/device items** — MAR 経路残
   - 非侵襲的陽圧換気(NPPV)741 records
   - 間欠的空気圧迫装置(IPC)154 records
   - inpatient.py daily order 生成箇所で detail 検査(RM-6b と同 pattern)を追加すれば解決

3. **GOLD 4 / asthma severity / HTN Stage / CCS SNOMED authoritative 検索** — session 40 policy
   継続、text-only
   - NLM CT / tx.fhir.org $lookup で追加 code 発見できれば追加、fabrication 禁止
   - RM-4 で GOLD 1/2/3 は追加できたが 4 は tx で見つからず

4. **vital-signs 22% missing performer** — 初回 review で 22% だった。RM-1 fix 後 0.0% だが
   別 subset(daily 追加 vs)の可能性、cycle 4 で再確認

### 中優先度

5. **YJ code MHLW verification chain** — 別 chain として、MHLW 薬価基準 令和6年 CSV master
   から 12-char authoritative YJ code を parse、code_mapping_drug.yaml 復元
   - 現状 text-only fallback で運用中(MR no-coding 48.9%)
   - large-scope、独立 chain 推奨

6. **社会歴 Observation.performer 100% missing** — SDOH は patient-level(intentional)
   - 現状 FHIR spec 準拠、患者自己申告時は performer 空でも OK
   - 議論余地:JP 保険医療機関では健診事務員が入力するので "recorder" 相当
     performer を入れる余地あり

### 低優先度 tail

7. Composition.section.code 残 8%(auto-derived title unmapped)
8. CareTeam 6-name expansion(β-JP-1、dietitian / PT/OT / MSW)
9. Encounter.location 詳細(現在 dept fallback、ward + bed 完備は次段階)
10. Facility bundle bypass の adapter post-hook(cycle 3 で発見、Organization は個別対応済み)

---

## ★★★ STEP 4:サイクル型監査ワークフロー(session 41 以降デフォルト)

Cycle 4 を開くタイミングで、以下を厳守:

1. **JP p=10000 seed=42 で CIF/FHIR 生成**
   ```bash
   python -m clinosim.simulator.cli generate --population 10000 --country JP --seed 42 \
       --output /private/tmp/claude-.../scratchpad/cycle-4/jp --format fhir-r4
   ```
   Cycle 2 以降は JP 先行 audit(多言語構造は維持、US/JP 両対応)。
2. **全体レビュー**: NDJSON を FHIR spec / display fallback / reference integrity /
   silent-drop / 統計異常 で監査。
3. **ランダムサンプリング**: 5-10 患者を全 FHIR resource review(データ品質 / 臨床整合 / リアリティ)。
4. **反復**: 2 + 3 を **合計 30 issues** 積み上げる。cycle 2 以降 30-rule(cycle 1 は 20)。
5. **改修**: 30 全て fix。**新規追加は「新規追加でしか解消できない」と判断された場合のみ**
   (memory `feedback_cif_fhir_quality_focus.md`)。**★ 各 fix ごとに sibling sweep 必須**
   (memory `feedback_check_sibling_bugs_across_modules.md`)。**★★ 勝手な in-cycle carry-over
   禁止**(session 42 user 明示、都度 user 確認)。
6. **再生成**: 同 seed で CIF/FHIR 再生成。
7. **回収検証**: 各問題点について「回収 / 未回収 / 新規発見」を判定・表示。
8. **★★ end-of-cycle 改修点レビュー**(session 42 user 明示、必須):regen + verify 後、
   authoritative source 未検証の code / URL / mapping は削除 or 別 chain に defer、
   user 報告 → 判断 → 対応。
9. **docs 更新**:
   - `docs/audit-cycles/cycle-<N>.md` 追記
   - `.session-resume-prompt.md` を cold-start 用に更新
   - `TODO.md` Status 反映
   - commit + push(全 clean)
10. **user 確認 = 2 選択肢**(このセッション継続 / 別セッション、自動継続禁止)。

### 進捗表示(必須)

`[Cycle 4 · n/30] <short desc>` を全 fix step で表示。サイクル終了時に
「回収 X / 未回収 Y / 新規発見 Z」を明示。

### Test 実行ポリシー(cycle 1 で確立)

- **各 fix ごと**: `pytest tests/unit -x`(~50s)
- **fix 段落ごと**: + `pytest -m regression`(+12s、golden ズレ検出)
- **cycle 再生成前 1 回**: + `pytest tests/integration -x`(+13 分)+ `pytest -m e2e -x`(+3 分)
- 反復中 integration/e2e を毎回回さない(subprocess 起動遅い)

### 判定基準(重要度順)

1. **データ品質**(FHIR spec 準拠、display fallback ゼロ、reference integrity)
2. **臨床整合性**(値の生理学的妥当性、時系列一貫性、疾患-検査-治療の因果)
3. **データのリアリティさ**(統計分布、稀な事象の発生率、臨床現場との一致度)

詳細: memory `feedback_audit_cycle_workflow.md`(★★★★ session 41 以降必読)。

---

## ★★★ STEP 5:作業方針(user 明示、session 40 wrap-up、session 41 以降デフォルト)

**CIF と FHIR の品質・整合性チェックとデバッグ、既存コードの修正を中心** に据える。
**新規追加は「FHIR 整合性の矛盾や異常を、新規追加をしなければ解消できない場合」に限定**。

### 新規追加時の 4 軸(user 明示、session 41 cycle 1 で確立)

1. プロジェクトコンセプト適合(YAML 駆動、CIF-only、AD-16 決定性、AD-30 code-only 等)
2. モジュール責任分解適切(境界/循環依存/single source of truth)
3. データ品質・臨床整合性適切(fabrication 回避、権威 source、spec 準拠)
4. 構造を極力シンプルに(sub-model 化回避、既存 helper 継承、大工事化時は user 確認)

### 「バグ 1 件見つけたら他 module も掃引」ルール(session 39 恒久ルール)

バグを 1 件見つけたら必ず全モジュールで同 class を grep 掃引・一緒に修正
(memory `feedback_check_sibling_bugs_across_modules.md`)。今後どのバグでも自動適用。

---

## ★★★ 必読 memory(cold-start 順)

1. **feedback_audit_cycle_workflow.md**(★★★★ session 41 以降サイクル型ワークフロー、最必読)
2. **feedback_cif_fhir_quality_focus.md**(★★★ 作業方針 + 新規追加ゲート)
3. **feedback_session_start_branch_check.md**(★★★ branch 同期、必読)
4. **feedback_own_background_verification_contamination.md**(★★★ session 37、自分の background
   検証コマンド実行中は編集しない)
5. **feedback_subagent_briefing_requirements.md**(★★★ subagent 委譲時の必須事項)
6. **feedback_subagent_worktree_and_background_task_pitfalls.md**(★★★ subagent 運用の罠)
7. **feedback_check_sibling_bugs_across_modules.md**(★★★ session 39 恒久ルール)
8. **feedback_recommendation_evaluation_axes.md**(★ 推奨提示時必ず使う 6 軸)
9. **project_session_42_end_state.md**(★ session 43 cold-start 起点、C2/C3 tail + RM-6/7 詳細)
   ※ session 43 開始時に作成 or 既存 project_session_XX_end_state series の最新参照
10. **project_fhir_completeness_roadmap.md**(session 38 主成果 = registry 台帳)
11. **project_realism_gaps.md**(6 件解消済み反映)
12. **feedback_scope_discipline.md** / **feedback_iterative_adversarial_review.md**

---

## ★★★ Communication

- 日本語 default。「進めて」「おすすめで」= 継続 execution 信頼委譲
- Scope discipline(★★★):明示的に「スコープを大きくせず」時は narrow scope + データ品質/
  臨床整合性向上を軸にした候補のみ提示
- Cycle 中は 30 issues 全 fix + regen + verify + **end-of-cycle 改修点レビュー** + docs +
  push + user 確認 まで完遂
- 勝手な in-cycle carry-over 禁止、都度 user 確認
- Chain workflow(session 42 実績:直接軽量 TDD 方式で cycle 2/3 + RM chain 完遂):
  - **直接軽量 TDD 方式**: recon → spec inline → TDD → 各 fix ごとテスト → 全 suite → commit →
    push。真の設計分岐点だけ user 確認
  - **subagent-driven-development**: 大規模タスク時
  - **5-lens adversarial review**: 独立検証が必要な時

### 修正のプロセス重量調整

修正の process weight は不確実性・リスクの大きさに合わせる — 既存パターンの単純適用に
毎回フル chain を回す必要はない(session 37/39/40/42 の実績:大量タスクを直接 TDD で完遂)。

---

## ★★★ 環境 / 状態

- **Primary working directory**: `/Users/tokuyama/workspace/clinosim`
- **Master HEAD**: `1f3f5b0fa8`(session 42 wrap、origin/master 完全同期済み)
- **Git**: working tree clean、未 commit なし、worktree 本体 1 つのみ
- **Scratchpad**: 0B(完全 cleanup 済み)
- **Tests**: unit 2,338 全 PASS / regression 12/12 PASS(session 43 終了時点)

---

## ★★★ Session 43 完全記録の場所

- `docs/audit-cycles/cycle-4.md` — cycle 4 完全記録
- `docs/audit-cycles/cycle-5.md` — cycle 5 完全記録(+ C1-C4 residual pass 記録)
- `docs/audit-cycles/cycle-3.md` — cycle 3 完全記録(session 42)
- `docs/audit-cycles/cycle-2.md` — cycle 2 完全記録(session 42)
- `docs/audit-cycles/README.md` — 監査サイクルワークフロー
- 本 prompt(`.session-resume-prompt.md`) — session 44 cold-start 用完全ガイド
- `TODO.md` Status セクション — 直近サイクル分の Status 要約
- git log — 全 commit history(session 40 以降は直接 master コミット方式)

Session 44 開始時、user から「開始」の指示があるまで自動継続はしない。
STEP 1 の必読 docs 把握 → 現状確認 → user 確認 → 次 chain opening、の順で進める。
