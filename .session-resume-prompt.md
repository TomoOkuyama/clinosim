# clinosim Session 26 Resume Prompt

下の `---` 〜 `---`(末尾)まで全部を、次セッション開始メッセージとして送ってください。

---

clinosim プロジェクトの作業再開です。**Tier 1 #3 α-min-1 Document Density Chain Foundation の SDD execution 着手**。セッション 25 で計画策定完了 → セッション 26 で 14-task SDD chain 起動 → 5-lens adversarial fan-out → CONVERGED → PR open までを目標。

## ★ 状態確認(セッション開始時、最初に必ず実行)

```bash
cd /Users/tokuyama/workspace/clinosim
git status                                       # working tree state
git log --oneline -10 master                     # master state(PR #127 merge 状態)
git log --oneline master..HEAD 2>/dev/null | head -10   # branch ahead 状態
git branch --show-current                        # 現 branch
```

**判断**:
- master HEAD に `Merge pull request #127` あり → PR #127 merged → SDD execution 着手 OK
- 無し → user に確認:**「PR #127 が未 merge ですが、本セッションで SDD execution に着手しますか? 並列 branch でも進行可能です」**

## ★ Strategic context(セッション 25 確立、最重要)

clinosim purpose = **EHR/EMR sample data generator**(医療現場で生成されるデータを利活用するシステム評価用大規模 dataset)。

**User 戦略確認(セッション 25 明示)**:
1. 病院 event 記録 density 充実(narrative + structured event 両 dimension)
2. データ品質 + 臨床整合性 維持
3. JP-specific は locale=JP gating
4. ★★★ **Scope discipline**:scope 拡大禁止、必須のみ scope 内 fix、それ以外 TODO 化

実測 gap(US p=10k imaging chain DQR):
- DocumentReference 0.00 件/encounter vs 実 EHR 200 床 JP 病院 5-15 件 = **完全空白**
- AllergyIntolerance / ClinicalImpression = **完全空白**

α-min-1 で foundation 確立 → 後続 6 phase で 200 床 JP 急性期 realistic density 達成(全 84-105 PR / 12-15 セッション規模)。

## ★ 必読 docs(セッション 26 最初に読む順)

| File | 役割 | 優先度 |
|---|---|---|
| `docs/superpowers/plans/2026-07-01-tier1-3-document-density-alpha-min-1-plan.md` | ★ 14-task SDD plan(本セッション execution 対象) | ★★★ |
| `docs/superpowers/specs/2026-07-01-tier1-3-document-density-alpha-min-1-design.md` | α-min-1 spec(scope discipline §14.5 含む) | ★★★ |
| `docs/design-notes/2026-06-30-tier1-document-and-event-density-master-plan.md` | Master plan(全 7 phase roadmap + Appendix A CIF gap 32 type) | ★★ |
| `MEMORY.md`(memory dir) | memory index | ★★ |

## ★ 必読 memory(セッション 26 最初に check)

| Memory | 役割 |
|---|---|
| **`feedback_scope_discipline.md`** | ★★★ scope 拡大禁止原則 + triage 判定基準(本 chain 絶対遵守) |
| **`project_session_25_end_state.md`** | セッション 25 末状態(再開起点) |
| `project_document_density_master_plan.md` | Master plan pointer |
| `project_event_density_strategy.md` | セッション 25 戦略軸転換 |
| `project_ehr_event_emphasis.md` | EHR event emphasis + SS-MIX2 延期 |
| `feedback_cif_to_fhir_no_drop.md` | no-drop invariant |
| `feedback_iterative_adversarial_review.md` | 5-lens fan-out 8 例 stable pattern |
| `feedback_recommendation_evaluation_axes.md` | 6 軸評価(推奨提示時) |
| `feedback_xhigh_review_lessons.md` | silent-no-op 7-layer + PR-90 lesson |
| `feedback_unify_data_logic.md` | canonical single source |

## ★ 14-task SDD chain(plan より)

1. CIF types(Allergy + NarrativeContext + ClinicalImpressionRecord)
2. Allergy module + allergens.yaml + POST_POPULATION enricher
3. Document module skeleton + DocumentTypeSpec registry + NarrativeContext factory
4. Disease YAML extension(narrative.* × 30 disease + Pydantic schema)
5. physical_exam_findings.yaml + discharge_instructions.yaml + 6-layer validators
6. TemplateNarrativeGenerator(3 format type 対応:FREE_TEXT + COMPOSITION + QUESTIONNAIRE_RESPONSE infra)
7. LLMNarrativeGenerator hook + cache + Idea D template-as-seed(default OFF、`CLINOSIM_NARRATIVE_LLM=on` で発火)
8. Document module engine.py(POST_ENCOUNTER enricher order=95 + ClinicalImpression daily generation)
9. 3 新 FHIR builders(`_fhir_composition` + `_fhir_allergy_intolerance` + `_fhir_clinical_impression`)
10. `_fhir_documents.py` refactor + 既存 narrative_generator/document_generator merge + builder registration
11. AD-60 audit + 15+ `lift_firing_proof` equality_checks
12. Integration tests + e2e golden + subprocess full-pipeline + JP localization + determinism
13. DQR(US p=10k + JP p=5k)+ 9 doc sync(README + MODULES + DESIGN AD-63 + CLAUDE + others)
14. Final whole-branch review + PR open

+ Post-chain converge → **5-lens adversarial fan-out**(pre-merge)

## ★ 進行手順 — 推奨

```bash
# 1. PR #127 merge 状態 OK 想定 / branch 作成
git checkout master
git pull origin master
git checkout -b feature/tier1-document-density-alpha-min-1
```

2. SDD execution skill 起動:**`Skill superpowers:subagent-driven-development`**(plan path = `docs/superpowers/plans/2026-07-01-tier1-3-document-density-alpha-min-1-plan.md`)

3. **「PR #127 merged、進めて」**(or **「進めて」**)= 推奨進行(branch checkout + 14 task SDD chain)

## ★ 設計判断 15 件 — user 承認済(spec §1.3)

実装中、brief 外項目で迷ったら本 list を参照:

| # | 判断 |
|---|---|
| 1 | Chain scope = α-min-1 単一 chain(splitしない) |
| 2 | Module = 新 `document/` + 新 `allergy/`、既存 narrative_generator + document_generator + _fhir_documents を document module へ merge |
| 3 | NarrativeContext location = `clinosim/types/document.py` |
| 4 | DocumentTypeSpec ownership = `clinosim/modules/document/narrative/registry.py` |
| 5 | Generator provider plugin = 既存 `llm_service.providers` wrap |
| 6 | Disease YAML field placement = top-level `narrative:` block |
| 7 | Disease YAML field set = 4 field 全部(hpi_template + physical_exam_findings + course_archetypes[].daily_trajectory + discharge_instructions) |
| 8 | Disease YAML fill strategy = baseline + 5 priority disease detailed + 残 25 generic |
| 9 | Allergy 範囲 = medication + food + environment 3 category |
| 10 | Allergy data source = `allergens.yaml` + `snomed-ct.yaml` |
| 11 | ClinicalImpression frequency = daily during stay(LOS-dependent) |
| 12 | FHIR builder file = resource ごと別 file |
| 13 | Stage 2 LLM = infrastructure + Idea D template-as-seed + Idea E cache(default OFF) |
| 14 | Locale gating = `DocumentTypeSpec.countries_supported`(registry-level filter) |
| 15 | PR slicing = 線形 14 PR、SDD subagent execution |

## ★ ★ Scope discipline 絶対遵守(memory `feedback_scope_discipline`)

User 明示(セッション 25):「スコープが拡大すると、永遠に終わらないので、スコープの拡大はせずに、データ品質や臨床的整合性の観点で必要な追加のみスコープ内で対応すること」

### Implementer / reviewer triage 基準

**★ Scope 内 fix(現 chain で対応)**:
- 既存 invariant 違反(canonical constant typo / ref dangling / no-drop 違反)
- データ品質 bug(silent-no-op / 不正値 emission / 計算誤り)
- 臨床整合性違反(矛盾 narrative / archetype 違反 / age-sex mismatch)
- 現 task の deliverable が成立しない必須追加

**OOS = TODO entry 化(現 chain で対応せず)**:
- Optimization / refactor / 単純性向上
- 新 feature の追加 / 既存 feature の拡張
- Reviewer の "consider adding" / "improvement suggestion"
- 後続 phase(α-min-2 / β-JP-1 / β-2 / γ / δ / ε)で計画済の項目

### Pre-merge 判定

- 残 issue が「data quality / clinical integrity 必須」のみ = ship-ready
- 残 issue に「scope 拡張要望」を含む = 拡張要望を ALL TODO 化、ship-ready 判定

## ★ Workflow リマインダ

### コミュニケーション

- すべての user-facing communication は **日本語で**(CLAUDE.md グローバル指示)

### Workflow

- master から branch 必須(`feature/tier1-document-density-alpha-min-1`)
- Commit trailer:`Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>` + `Claude-Session: <session-url>`
- SDD execution = 各 task implementer dispatch → reviewer dispatch → fix(必要なら)→ re-review → mark complete → next task
- Adversarial fan-out 必須(post-chain、5-lens parallel = 8 例 stable pattern)
- Pre-merge gate:`pytest tests/unit tests/integration -m "unit or integration"` full sweep(session 22 教訓)

### Code 権威 sources

- LOINC = NLM clinicaltables.nlm.nih.gov + JCCLS-JSLM v137
- SNOMED = tx.fhir.org `$lookup`
- 不確実コードは `# TODO: verify` 残し

### Confirm before destructive actions

- 削除 / force-push / branch overwrite 等の destructive 操作は user 明示承認後
- TODO.md / Master plan / spec / plan は **append-only** が原則(rewrite は user 承認後)

## ★ 補足情報

- TODO 残:imaging chain 5-lens adversarial fan-out(TODO #3、PR #127 merge 後)= 本 chain と並列可能(optional)
- SS-MIX2 output adapter = TODO 確定、将来 phase 候補(user 明示延期)
- 本 chain は Tier 1 #3 全体(7 phase / 84-105 PR)の **第 1 phase(α-min-1)**= foundation 確立
- 後続 α-min-2 〜 ε は master plan §2 に scope 確定済

---
