# Session 43 Resume Prompt(2026-07-08+、Cycle 3 CLOSED)

## ★★★ Cycle 3 CLOSED、Cycle 4 開始待ち(user 確認待ち)

**Cycle 3 結果**(session 42、`docs/audit-cycles/cycle-3.md` 参照):
- **20 issues fully RESOLVED**(CO-4/5、CY2-A/C、C3-01/02/06/07/08/10/11..18)
- **4 issues fixed with partial coverage**:
  - C3-03/04 Immunization lot/performer(status=completed のみ、not-done 539 は intentional)
  - C3-05 reasonCode 100%
  - C3-10 multi-word drug base(38% ↓、残 procedure/device = CY2-B)
- **CO-3/7/9**: infrastructure ready(builder-level 修正済)、CIF data-side は minimal effect
- **6 issues in-cycle attempted、larger-scope 要**(cycle 4 candidate、carry-over 判断は user 確認)
  - CO-1 ImagingStudy density(10+ diseases × YAML 拡張)
  - CO-2 JP chronic multiplier(simulator population-model 調査)
  - CO-6 stage SNOMED tail(session 40 no-fabrication policy 継続)
  - C3-09 Observation.performer cross-cut(5+ builder edits)
  - CY2-B MR/MAR classification(CIF Order → Procedure/Device dispatch refactor)
  - CO-8 (C2-15) YJ code(authoritative MHLW verification 別 chain)
- **User-approved deferrals**: 2(CO-6 no-fabrication、CO-8 YJ code)

**Cycle 2 review outcome**(session 42 cycle 3 中で実施):
- YJ code fabrication risk 特定 → NOTE 強化、別 chain で権威検証(user 承認)
- JP Core URL 13 個 WebFetch 検証(全て authoritative)
- LOINC section codes / Coverage.period fiscal year 修正
- 12 fix items 権威 source 確認済

**Cycle 4 開始時にやること**(session 43 開始 = user 承認後):
1. Cycle 3 attempted-defer 6 件を cycle 4 opening list に追加(user 承認前提)
2. JP p=10000 再生成 → 全体レビュー + サンプリングで **30 まで積む**
3. 30 全て改修 → JP 再生成 → 検証 → docs 更新 → 継続確認

## Session 42 完了状態

- master HEAD: cycle 3 CLOSED commit(session 43 開始時に `git log` で確認)
- Cycle 3 scratchpad: `/private/tmp/claude-.../scratchpad/cycle-3/`(次サイクル削除可)
- **主要成果物**:
  - adapter-level `_apply_jp_core_profile(resource)` + `_JP_CORE_PROFILES` dict
    (13 JP Core URL 全て jpfhir.jp WebFetch 検証済)
  - Encounter.location AMB/EMER に dept Location fallback(facility bundle
    dept Location 追加)
  - SR.code.system JLAC10 routing(CO-4 完了、imaging SR 1,781 intentional LOINC)
  - CareTeam.participant.role SNOMED(309343006 physician / 224535009 nurse /
    46255001 pharmacist)
  - Immunization lot/performer/reason 構造化(lot は stub NOTE 明示)
  - AllergyIntolerance recorder + recordedDate(recorder = first encounter
    attending / recordedDate = onsetDateTime or admission fallback)
  - Coverage.class[] + fiscal year period(4/1〜3/31)
  - Composition section.code 20 追加 LOINC マッピング(98.8% coverage)
  - MR/MAR multi-word drug base lookup(longest-match-wins、Normal saline
    等 space-separated variants YAML 追加)
  - ED 6 procedure(ecg_12lead / iv_line / wound_care / short_arm_splint /
    reduction_closed / oxygen_therapy)+ 6 ED procedure rule
  - Practitioner.name JP kanji IDE valueCode extension

---

# サイクル型監査ワークフロー(user 明示、session 41 以降デフォルト)

**session 43 開始と同時に Cycle 4 を開いて、以下を厳守すること**:

1. **CIF/FHIR 生成**: **Cycle 2 以降は JP を先行 audit 対象**(user 明示)。
   `python -m clinosim.simulator.cli generate --population 10000 --country JP
   --seed 42 --output <path> --format fhir-r4`(サイクル内で一度)。レビューは
   **JP 医療機関適合性 + JP Core FHIR プロファイル準拠** を優先観点に、データ品質 /
   臨床整合性 / リアリティ の 3 軸で。
2. **全体レビュー**: 生成 NDJSON を FHIR 準拠性 / display fallback / reference
   integrity / spec 違反 datetime / silent-drop / 統計異常等の観点で監査。
3. **ランダムサンプリング**: N 患者(5-10)をランダム抽出、その患者の全 FHIR リソースを
   1 人ずつ「データ品質 / 臨床整合性 / データのリアリティさ」観点でレビュー。
4. **反復**: 2 + 3 を **合計 30 問題点** リストアップされるまで繰り返す
   (cycle 2 以降 30-rule)。
5. **改修**: 30 問題点をすべて改修。**新規追加は「新規追加でしか解消できない」と
   判断された場合のみ**。**★ 各 fix ごとに sibling sweep 必須**。
   **★★ 勝手に carry-over 判断禁止(session 42 user 明示)** — サイクル内で
   直すかどうか困ったら都度 user 確認。
6. **再生成**: 改修後、同 seed / 同 population で CIF/FHIR 再生成。
7. **回収検証**: 各問題点について「回収 / 未回収」を判定・表示。
8. **サイクル切れ目の docs 更新 + resume prompt 記録**(必須):
   - `docs/audit-cycles/cycle-<N>.md` に検証結果追記
   - `.session-resume-prompt.md` を cold-start 用に更新
   - `TODO.md` の Status セクションに進捗反映
   - commit + push 完了
9. **サイクル継続確認**: docs 更新 + push + cleanup 完了時点で
   user に「このセッションで続けるか / 別セッションで続けるか」の 2 選択肢 +
   回収状況 + carry-over 数を報告(自動継続禁止)。

### ★★★ Cycle 内の scope 判断ルール(session 42 user 明示)

**"完結型" cycle 目標**:cycle 内で 30 items 全てを何らかの形で fix する
(user 明示、session 42 cycle 3)。**勝手に carry-over しない**。困ったら
「サイクル内で直すか / 見送るか」を user に確認する。

**改修点レビュー中間ゲート**(user 明示、session 42):
fix 途中で「何を改修したか、なぜそう改修したか、リスクは何か」を明示
レビュー → user 確認 → 続行 or 修正、を通す。

進捗表示 `[Cycle N · n/30] <short desc>` を全 fix step で表示。
サイクル終了時に「回収 X / 未回収 Y / 新規発見 Z」を明示。

### 判定基準(重要度順)

1. **データ品質**(FHIR spec 準拠、display fallback ゼロ、reference integrity)
2. **臨床整合性**(値の生理学的妥当性、時系列一貫性、疾患-検査-治療の因果)
3. **データのリアリティさ**(統計分布、稀な事象の発生率、臨床現場との一致度)

## ★★★ 作業方針(user 明示、session 40 wrap-up、session 41 以降デフォルト)

**CIF と FHIR の品質・整合性チェックとデバッグ、既存コードの修正を中心** に据える。
**新規追加は「FHIR 整合性の矛盾や異常を、新規追加をしなければ解消できない場合」に
限定**。

- **新規追加時の 4 軸**(user 明示):
  1. プロジェクトコンセプト適合(YAML 駆動、CIF-only、AD-16 決定性、AD-30 code-only 等)
  2. モジュール責任分解適切(境界/循環依存/single source of truth)
  3. データ品質・臨床整合性適切(fabrication 回避、権威 source、spec 準拠)
  4. 構造を極力シンプルに(sub-model 化回避、既存 helper 継承)

## ★★★ セッション開始直後、必ず実行すること

```bash
cd /Users/tokuyama/workspace/clinosim
git branch --show-current
git log --oneline -1 master
git log --oneline -1 origin/master
git status --short
git worktree list
```

master でなければ `git switch master`。 前セッション(42)は master 完全同期済み。

## ★★★ 作業再開前に必ず読むこと

1. `docs/design-guides/project-concept-and-design.md` — コンセプト9要求
2. `docs/design-guides/implementation-rules.md` — 不変則 + §6.5 共通ロジック所在
3. `MODULES.md` — 30 モジュール一覧
4. `CLAUDE.md` — コード規約
5. `docs/audit-cycles/cycle-3.md` — 直前サイクル完全記録
6. `docs/audit-cycles/cycle-2.md` — cycle 2 review + fix 詳細

## Communication

- 日本語 default
- Cycle 中は 30 issues 全 fix + regen + verify + docs + push + user 確認 まで完遂
- Cycle 内での「勝手な carry-over」禁止、都度 user 確認
- Chain workflow: 直接軽量 TDD 方式 or 5-lens adversarial review or
  subagent-driven-development(タスク規模で選択)
