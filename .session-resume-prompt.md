# Session 31 Resume Prompt(2026-07-04+)

## Session 30 で完了(cold-start context)

- **★ PR #132 opened** — Tier 1 #3 α-min-2c Canonical Patient Profile Fixture Library(AD-66)
  - PR URL:https://github.com/TomoOkuyama/clinosim/pull/132
  - branch:`feature/tier1-3-alpha-min-2c-fixture-library`
  - branch HEAD:`b633bcf3fc`
  - merge base:`b31561ce2c`(session 29 resume prompt commit、post-PR #131)
  - master HEAD:`b31561ce2c`(unchanged since session 29)
  - **17 commits total** = 2 spec commits + 1 plan commit + 13 SDD task commits + 1 ruff auto-fix
  - **1565 unit + 6 regression + 257 integration PASS、0 regressions**

- **★ AD-66 architecture = β-JP-1 LLMNarrativePass regression testing 基盤**
  - `PatientProfile` Pydantic type(`clinosim/types/config.py`、extra=forbid、Literal enums)
  - `PatientProfile.to_forced_scenario() → ForcedScenario` transform
  - `tests/fixtures/patient_profiles/<name>.yaml` + `<name>.golden.json` sibling layout
  - `test-disease --patient-profile <name-or-path>` CLI(positional disease_id optional、CLI override stderr WARN)
  - `clinosim regenerate-goldens [--profile <name>|--all]` CLI(idempotent、subprocess pipeline invoke)
  - `pytest -m regression` opt-in suite(byte-diff vs golden、unified diff on mismatch)
  - CLAUDE.md AD-66 rules 1-2:YAML+golden ship together、unexpected diff = regression suspicion

- **6 initial disease-based inpatient/ICU profiles(all deterministic at seed 42)**:
  - #1 `jp_inpatient_bacterial_pneumonia`(bacterial_pneumonia moderate smooth_recovery、26 docs)
  - #2 `us_inpatient_acute_mi`(acute_mi severe plateau、12 docs)
  - #3 `jp_icu_sepsis_hai_clabsi`(sepsis severe dip_then_recovery + `force_hai_event=CLABSI S.aureus`、76 docs)
  - #4 `us_inpatient_diabetic_ketoacidosis`(severe smooth_recovery、10 docs)
  - #5 `jp_inpatient_copd_exacerbation`(moderate dip_then_recovery、24 docs)
  - #6 `us_inpatient_hemorrhagic_stroke`(severe dip_then_recovery、10 docs)

## ★★ 特筆事項 7 選(次 session に伝えるべき)

1. **★★★ Spec correction pre-plan pattern**:brainstorming 段階で 6 profile mix を確定したが、実 disease registry と 3 件 mismatch(disease_id 命名 + 4 archetype 全て非存在 + DM 非 disease-module)。plan 書き始め時に `ls clinosim/modules/disease/reference_data/` + archetype grep で verification → spec correction commit で修正 = 正常 recovery。**Lesson:brainstorming outcome を plan 化する前に必ず real registry で verify、次回はより早く**。

2. **★★ Scope discipline による ED/outpatient profile deferral(session 25/29 pattern 継続)**:`run_forced` は `load_disease_protocol` 経由 disease module only = ED/outpatient encounter conditions は走らない。symmetric `test-encounter --patient-profile` extension が fresh scope。TODO.md に明示 defer(β-JP-1 or 後続 chain)。

3. **★★ Pre-existing structural CIF wall-clock nondeterminism 発見(F-2 CLI 決定性 test)**:`issue_date` + MAR `timestamp` が `datetime.now()` 使用 = AD-16 discipline に反する。narrative は `_deterministic_timestamp` で fully deterministic。テストは narrative 比較に変更、structural CIF cleanup は post-α-min-2c TODO 化。**Lesson:legacy 構造化 CIF paths に wall-clock timestamp が残っている、次 chain で cleanup**。

4. **★★ Inline vs subagent execution 判断基準(session 30 pattern precedent)**:session 26-29 は subagent-driven pattern、session 30 は inline TDD で執行。判定軸:scope size(~800 lines + 6 straightforward YAMLs)+ TDD 順序 strict + auto mode = inline 効率的。大規模 chain(15+ tasks、cross-task dep)は subagent-driven 継続推奨。

5. **★★ 6 profile golden counts wide range(10-76 docs)**:sepsis+HAI は最大 76、DKA/hemorrhagic は最小 10。narrative 密度は disease + archetype + severity の関数。**β-JP-1 LLM regression cost 見積もり時に有用**:6 profile 合計 ~158 docs × N sections × 3 models = ~2000-5000 LLM calls per full regression。

6. **★ CLINOSIM_PATIENT_PROFILE_DIR env var**:test isolation のため env var 経由で override 対応。production は `_PATIENT_PROFILE_DIR = Path(__file__).parent.parent.parent / "tests" / "fixtures" / "patient_profiles"` を default。unit test は monkeypatch or env var 両方使える。

7. **★ ruff --fix idempotency + blame check pattern**:8 remaining ruff warnings は全 pre-existing(blame commit pre-α-min-2c)= my code は ruff-clean、scope discipline で放置。auto-fix で 4 件対応 + 4 削除された unused imports 対応 = 1 commit で cleanup。

## Session 31 開始点

### 状態確認(最初に必ず)

```bash
cd /Users/tokuyama/workspace/clinosim
git status
git log --oneline -5 master
gh pr view 132 --json state,mergeable
# 期待: master HEAD = b31561ce2c(session 30 resume prompt commit or 本 commit)
# 期待: PR #132 = OPEN or MERGED
```

### 推奨進行 3 選択肢

#### 選択肢 A(推奨):PR #132 adv-1 5-lens fan-out(12 例目 chain converged 目標)
Session 26/27/28+29 pattern 継続:
1. adv branch = master fork(session 29 pattern 踏襲)
2. review-package generate(`b31561ce2c..b633bcf3fc` = 17 commits)
3. 5 parallel adversarial reviewers:
   - Lens 1: silent no-op(regenerate idempotency、profile_id/filename mismatch、determinism claims)
   - Lens 2: data unification(FIXTURE_DIR constant、`_PATIENT_PROFILE_DIR` single source)
   - Lens 3: FHIR/JP Core(N/A — no new FHIR emission、but verify no regression)
   - Lens 4: determinism + scale(narrative byte-diff vs golden、AD-16 discipline)
   - Lens 5: spec + memory + CLAUDE.md(AD-66 rules 1-2 enforced、TODO deferred entries correct)
4. Findings 集約 + triage → 1 fix subagent → adv-2 → merge → 12 例目 converged

#### 選択肢 B:PR #132 直接 review + merge
Inline TDD で each task に test-first、pre-existing structural CIF nondeterminism も明示 TODO 化済、scope 小(17 commits)= adv-1 skip 妥当性 exists。session 27 α-min-2 pattern に近い。

#### 選択肢 C:β-JP-1 LLMNarrativePass chain start
α-min-2c で foundation 完成 = β-JP-1 unblocked:
- Bedrock Sonnet-4 + Ollama qwen:7b provider
- `<profile>.llm-<model>.golden.json` parallel storage(6 profiles × N models)
- Semantic diff mechanism(fuzzy match / tolerance thresholds / expected phrases)
- `NarrativeOutput.metadata.get("generator")` + `doc_status` field 拡張(session 27 memory consideration #4-#5)
- `narrate --patient-filter <regex>` for iterative LLM tuning

## 必読 memory(cold-start 順)

1. **project_session_30_end_state.md**(★★★ この session 完了 + 7 特筆事項 + PR #132 details)
2. **project_session_29_end_state.md**(PR #131 merged + 11 例目 chain converged + adv-1 5-lens summary)
3. **project_session_28_end_state.md**(AD-65 SDD 完了 + 4 bug fixes)
4. **project_session_27_end_state.md**(α-min-2 完了 + Stage 2 architecture 8 considerations)
5. **project_session_26_end_state.md**(α-min-1 完了 + adv-1 pattern precedent)
6. **feedback_iterative_adversarial_review.md**(4-stage chain pattern)
7. **feedback_scope_discipline.md**(★★ scope 拡大禁止、triage 判定基準)
8. **feedback_recommendation_evaluation_axes.md**(6 軸評価)
9. **feedback_cif_to_fhir_no_drop.md**(no-drop invariant)
10. **feedback_unify_data_logic.md**(canonical single source)
11. **feedback_xhigh_review_lessons.md**(PR-90 教訓、`PatientProfile` extra=forbid で validation)
12. **project_document_density_master_plan.md**(全 7 phase roadmap)

## 必読 docs(master HEAD `b31561ce2c` 反映 + PR #132 未 merge)

- `docs/superpowers/specs/2026-07-03-tier1-3-alpha-min-2c-fixture-library-design.md`(AD-66 design 完全版、on feature branch)
- `docs/superpowers/plans/2026-07-03-tier1-3-alpha-min-2c-fixture-library-plan.md`(13 SDD tasks、on feature branch)
- `TODO.md` `Post-α-min-2c fixture library extensions` section(7 items、on feature branch)
- `DESIGN.md` AD-66 ADR(on feature branch)
- `CLAUDE.md` AD-66 rules 1-2(on feature branch)
- `tests/fixtures/patient_profiles/README.md`(profile 追加 workflow、on feature branch)

## Deferred TODO(post-session-30)

### 高優先(TODO.md `Post-α-min-2c fixture library extensions`、7 items)
- ED / outpatient encounter-based profiles(`test-encounter --patient-profile` symmetric CLI extension 要)
- LLM semantic diff mechanism(β-JP-1 scope — byte-diff insufficient for LLM output)
- Clinical review loop(profile 別 physician + nurse validation)
- CI GitHub Actions workflow(automated regression at PR time)
- LLM parallel goldens(`<profile>.llm-<model>.golden.json`)
- Pre-existing structural CIF wall-clock nondeterminism(issue_date / MAR timestamp)
- Additional disease-based profiles beyond α-min-2c 6

### 中優先(session 29 AD-65 adv-1 deferred = 依然 valid)
- Practitioner/UNKNOWN dangling ref(broader design)
- Patient/ empty-id dangling ref
- Bug A archetype YAML structural gap
- `_deterministic_timestamp` per-doc mix
- Re-narrate orphan file cleanup
- β-JP-1 NarrativeOutput.metadata generator hook + doc_status field slot
- Encounter YAML `_en/_ja` peer CI enforcement
- `current_version.txt` write helper DRY

### 低優先
- Section title JP localization
- audit.py pre-existing Ruff I001 + 4× E501
- MODULES.md 細かい module 数 reconciliation

## Deferred to future phases(master plan §2)

- **β-JP-1**:LLMNarrativePass + 入院診療計画書 / 看護必要度 D 表 / QuestionnaireResponse active + 多職種 staff allocation
- **β-2**:手術記録 / 麻酔記録 / MedicationDispense
- **γ**:MSW / Appointment / Communication
- **δ**:Pathology / CarePlan / 死亡診断書
- **ε**:ADT / per-dose MAR

## Communication

- 日本語 default(CLAUDE.md グローバル指示)
- Auto Mode 傾向:user 「進めて」「おすすめで」「続けて」= 継続 execution 信頼委譲(session 26-30 で複数回確認)
- Scope discipline(★★★):spec 確定後 scope 拡大禁止、data quality/clinical integrity 必須のみ scope 内、それ以外 TODO entry 化(session 30 で守れた:7 items deferred)
- Inline execution も subagent execution と同等に有効(session 30 pattern precedent)

## 進行トリガ(コピペ可)

user 「memory 確認 → 開始」:memory 12 件 Read → 選択肢 A/B/C の推奨提示

user 「A」or「adv-1」or「5-lens fan-out」= 選択肢 A、adv branch 作成 + 5-lens dispatch

user 「B」or「PR merge」or「直接 merge」= 選択肢 B、PR #132 review + merge

user 「C」or「β-JP-1」or「LLM narrate」= 選択肢 C、brainstorming 起動

user 「進めて」or「おすすめで」or「続けて」= session 29 pattern 継続 = 選択肢 A(adv-1)推奨、user 判断待ち
