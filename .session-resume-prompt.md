# Session 32 Resume Prompt(2026-07-03+)

## Session 31 で完了(cold-start context)— 4 PR merged、master = `3448e15891`

1. **PR #132 MERGED**(α-min-2c fixture library、AD-66)— compact adv review → 3 IMPORTANT fix 後 merge
2. **PR #133 MERGED**(common-logic unification + guide refine)— byte-identical R1-R7、
   `is_jp`/`resolve_lang`/`system_key_for`、`docs/design-guides/README.md` 新設
3. **PR #134 MERGED**(α-min-3 nursing 3-shift)— stale TODO 実測検証で scope 縮小、
   `daily_3shift`(深夜/日勤/準夜)、goldens 6 profile 再生成
4. **★ PR #135 MERGED**(N-chain narrative IF unification)— **β-JP-1 の前提が全て完了**:
   - `NarrativeGenerator` Protocol(types/document.py、`DocumentTypeSpec` も移設)+
     `NarrativePass` constructor 注入 + **`LLMNarrativePass` 新設**
   - **CLI `narrate --provider bedrock|ollama|mock` が動く**(`--llm-config` override、
     mock は YAML 不要、非 template provider は `--set-current` default off)
   - LLM 呼出しの唯一 seam = `LLMService.complete_prompt(system, user, *, language,
     task_type, ...)`(raise 契約 `LLMCompletionError`、fallback は
     `LLMNarrativeGenerator` の責務、retry+PromptCache は `_complete_with_retry` 共有)
   - 戦略 D+E+B(template-as-seed + 2 層 cache + section-level)が production spec
     (admission_hp: hpi/assessment_and_plan、discharge_summary: hospital_course/
     discharge_instructions)で実動作(mock 実証済)
   - prompt 所有権 = `llm_service/prompts/{en,ja}/*.yaml` のみ(`narrative_seed.yaml` 新設)
   - enum sync: `DocumentType ⊆ LLMTaskType(NARRATIVE)` import-time validation
   - **adv-1 C-1 教訓(PR-90 級)**: `demographics_bucket` が dict patient で退化し cohort
     全体 1 cache entry 共有 → `get_attr_or_key` 化 + cache key に `template_seed_hash`
     (hit ⇔ seed 一致で患者間コンタミ構造的に不可能)
   - spec: `docs/superpowers/specs/2026-07-02-n-chain-narrative-if-unification-design.md`

## Session 32 開始点

### 状態確認(最初に必ず)

```bash
cd /Users/tokuyama/workspace/clinosim
git status && git log --oneline -3 master
# 期待: master = 3448e15891(PR #135 merge)or 本 resume prompt commit
```

### 推奨 = β-JP-1 LLMNarrativePass 本格化(blocker ゼロ)

infrastructure は #135 で完成済。β-JP-1 の実作業(master plan §2 + session 30/31 の考察):
1. **実 provider 品質**: Bedrock(`config/llm_service.bedrock.yaml`、EC2 52.195.159.66 =
   memory reference_ec2_access)/ Ollama(qwen:7b)で `narrate --provider bedrock` を
   6 fixture profiles に対して実行 → prompt 品質チューニング(ja 書式規約【】/■/・)
2. **LLM golden**: `<profile>.llm-<model>.golden.json` 並行保存 + semantic diff mechanism
   (byte-diff は LLM 出力に不適 — fuzzy match / tolerance / expected phrases)
3. **JP 厚労省 4 帳票**: 入院診療計画書(LOINC 18776-5)/ 看護必要度 D 表 / 栄養管理計画書 /
   リハ計画書(QuestionnaireResponse active 化 + NutritionOrder 基盤)
4. **多職種 staff allocation**(CareTeam 6-name、Practitioner roster 拡張)
- 規模: ~2 sessions。1(provider 品質 + golden)と 3(帳票)は独立 chain に分割可

### N-chain 残 TODO(TODO.md "N-chain adv-1 deferred" + spec §6)

- N-4: template_generator.py(1490 行)の section template YAML 化(段階的)
- `_build_context` の disease_protocol=None / archetype "" / day_index 0 配線
  (template seed の文章品質を上げる前提でもある — β-JP-1 chain 1 と同時が効率的)
- `get_default_cache()` dead seam 整理

### その後の chain(grand-design note §4)

determinism chain(wall-clock 全除去 + JP blood_type 重み 0.9999999999999999 修正)→
AD-30 chain(display-in-CIF 除去)→ display-dict → codes YAML 移行

## 必読 memory(cold-start 順)

1. **project_session_31_end_state.md**(★★★ 4 PR + N-chain 詳細 + C-1 教訓)
2. project_document_density_master_plan.md(7 phase roadmap)
3. feedback_scope_discipline.md / feedback_xhigh_review_lessons.md /
   feedback_iterative_adversarial_review.md
4. reference_ec2_access.md(Bedrock EC2)+ project_llm_status.md

## 必読 docs

- `docs/superpowers/specs/2026-07-02-n-chain-narrative-if-unification-design.md`
- `docs/design-notes/2026-07-02-grand-design-review-and-roadmap.md`
- `TODO.md` § "N-chain adv-1 deferred" + § "Common-logic unification review — deferred chains"

## 教訓(session 31)

- stale TODO は着手前に実測 status audit(α-min-3 で 1 PR 分回避)
- dict/dataclass dual-access ルールは cache key 経路も例外でない(C-1)
- compact adv review(1-agent 5-lens)は中規模 PR に有効(#132: 3 IMPORTANT、
  #135: 1 CRITICAL + 2 IMPORTANT、全て実証付き検出)

## Communication

- 日本語 default。「進めて」「おすすめで」= 継続 execution 信頼委譲
- Scope discipline(★★★): spec 確定後 scope 拡大禁止、必須のみ scope 内、他は TODO entry 化
