# Session 35 Resume Prompt(2026-07-04+)

## ★★★ セッション開始直後、必ず実行すること

```bash
cd /Users/tokuyama/workspace/clinosim
git branch --show-current
git log --oneline -1 master
```

現在ブランチが `master` の祖先(`git merge-base --is-ancestor HEAD master` が true)なら
**即 `git switch master` してから** 状態確認・作業を開始すること。session 33 で、作業ツリーが
前セッションの古い feature ブランチのままセッションが始まり、この `.session-resume-prompt.md`
自体を古い内容のまま読んでしまい、既に完了していた作業へ二重着手しかけた事故が発生した
(詳細: memory `feedback_session_start_branch_check.md`)。resume prompt も TODO.md も、
checkout されているブランチに依存するファイルシステム上の内容でしかない — 鵜呑みにせず
まず branch を合わせる。

**master HEAD は session 34 終了時点で `4fac95a4ad`。** これより古い場合は fetch/pull を
検討すること(ただしユーザーの明示的指示なしに push/pull は行わない)。

## ★★★ subagent へタスクを委譲する際の必須事項

Agent tool / Workflow / subagent-driven-development 等で実装 task を subagent に渡す時、
plan の task 文面を渡すだけでは不十分。**dispatch prompt の本文に毎回明示すること**
(参照だけでなく書く):

1. `docs/design-guides/implementation-rules.md` を実装前に読むこと
2. その task が模倣すべき既存 precedent ファイルの具体的 path
3. 再実装禁止の canonical single-source helper 一覧(`_o()` / `code_lookup` /
   `is_jp()` / `resolve_lang()` 等)
4. その task に関連する具体的 ADR 番号(AD-16 決定性、AD-30 CIF code-only、
   AD-55/56 モジュール境界 等)
5. **(session 34 追加)** worktree で作業する subagent には「絶対パスを全ての
   Read/Edit/Write 呼び出しで使うこと」+「過去にこの repo で worktree でなく main
   リポジトリへ誤って commit する事故が複数回発生している」ことを明示。session 34 では
   これを入れた後半 chain(AD-30)で事故が 0 件だった。詳細: memory
   `feedback_subagent_worktree_and_background_task_pitfalls.md`

session 33 でユーザーから直接指摘(詳細: memory `feedback_subagent_briefing_requirements.md`)。

## Session 34 で完了(cold-start context)— master = `4fac95a4ad`

**2チェーン完了、両方 subagent-driven-development(worktree分離)で実施、両方 master へ
ローカルマージ済み。** 詳細: memory `project_session_34_end_state.md`。

### 1. Determinism chain(wall-clock除去、7タスク)— commit `34a4a46f1a`..`9b590deb2d`

`default_factory=datetime.now/date.today` を固定sentinel定数に置換(5ファイル+実行中に
`HospitalState.timestamp` も追加発見)、`PhysiologicalState.timestamp` と
`PrescriptionRecord.issue_date` に実時刻を配線、immunization enricher の `date.today()`
フォールバックを fail-loud 化、JP blood_type sampling を `normalize_probabilities` 経由に
統一。spec: `docs/superpowers/specs/2026-07-04-determinism-chain-wallclock-removal-design.md`
(session 34 addendum あり — sentinel方式への転換経緯)。

### 2. AD-30 chain(display-in-CIF除去、6タスク)— commit `86aad16112`..`4fac95a4ad`

`AllergyReaction.manifestation_display`/`ImagingSeries.body_site_display`(deadフィールド)
削除、`Allergy.allergen_display` 削除 + 唯一の live 消費箇所(narrative生成
`template_generator.py:_build_allergies`)を `code_lookup` 経由に修正(修正しないと全生成
臨床文書のアレルギー欄が黙って空白化する回帰だった — 実FHIR出力で SNOMED 256262001 が
正しく "Tree pollen"/"花粉" に解決されることを独立確認済み)、`_validate_allergens`/
`_validate_body_sites` を SNOMED registry 照合まで拡張。
spec: `docs/superpowers/specs/2026-07-04-ad30-display-in-cif-removal-design.md`。
**注意**: chain完了後、TODO.md 自身の「AD-30 chain」エントリを削除するステップを plan に
含め忘れており、session 34 末に手動クリーンアップした。次回同種chainの plan 作成時は
「完了時に自チェーンの TODO.md エントリを消す」ステップを必ず含めること。

### 新規 TODO.md エントリ(次回以降の対応候補、session 34 で追加)

- `PrescriptionRecord.issue_date` 精度ギャップ(discharge時 `admission_time` 暫定使用)
- Dead Bundle-timestamp footgun(`_fhir_facility.py:159`, `fhir_r4_adapter.py:456`)
- Allergy/imaging display locale-freeze(`allergens.yaml`/`body_sites.yaml` の `_ja` 未使用)

### subagent-driven-development 運用教訓(★必読)

memory `feedback_subagent_worktree_and_background_task_pitfalls.md` 参照。
worktree 誤commit事故対策 + テスト完了後の background 待機ループへの対処法。

## Session 35 開始点

### 推奨作業候補

1. **chain 2継続(リハビリテーション計画書)** — 厚労省4帳票の残り1つ。新規rehabセッション
   サブシステム構築(`extensions["procedure"]`)が先行必要、規模大。
2. ~~N-chain状態要検証~~ → **session 35 で検証完了、DONE。** TODO.md の★★★表記は stale
   だった。N-1/N-2/N-3、および adv-1 の `_build_context` degenerate-fields 項目まで全て
   session 31-32 で実装済み(commit `5e7077f0d9`/`c981c390e2`/`3da54aaeb6`/`38b4b32f31`/
   `45b4899c1e`)、β-JP-1 は session 31 時点で unblocked 済み。TODO.md を修正し、残 2 項目
   (`get_default_cache()` dead seam 削除 / N-4 optional data駆動化)のみ残存として記載し直し
   た。次に着手するなら item 1・3、または新規タスク。
3. その他: CareTeam多職種拡張(6-role staff roster)、看護必要度評価票(規模大、
   適切なLOINCコードなし要調査済み — `docs/superpowers/specs/2026-07-03-nutrition-care-plan-design.md`
   §1参照)。

方向性はユーザーに確認してから着手すること(session 33/34共通のパターン)。

## 必読 memory(cold-start順)

1. **feedback_session_start_branch_check.md**(★★★ branch同期、必読)
2. **feedback_subagent_briefing_requirements.md**(★★★ subagent委譲時の必須事項)
3. **feedback_subagent_worktree_and_background_task_pitfalls.md**(★★★ session 34 新規、
   subagent運用の罠)
4. project_session_34_end_state.md(本session詳細)
5. feedback_scope_discipline.md / feedback_iterative_adversarial_review.md /
   feedback_recommendation_evaluation_axes.md
6. project_document_density_master_plan.md

## 必読 docs

- `docs/design-guides/project-concept-and-design.md` — コンセプト9要求/パイプライン全体像
- `docs/design-guides/implementation-rules.md` — 不変則の蒸留版(★subagent委譲時は
  この2文書を明示的に読ませること)

## Communication

- 日本語 default。「進めて」「おすすめで」= 継続execution信頼委譲
- Scope discipline(★★★): spec確定後scope拡大禁止、必須のみscope内、他はTODO entry化
- Chain workflow(確立済、2バリエーション運用中):
  - **5-lens adversarial review方式**: recon → spec → TDD実装 → 独立検証 → PR →
    compact adv review(1-agent 5-lens、実証要求)→ fix → 全suite → merge
  - **subagent-driven-development方式**: recon → spec → plan(writing-plans、
    precedent file/canonical helper/ADRを明示調査した上でtask化)→ worktree分離 →
    task毎に実装subagent+reviewer subagentの2段ゲート → 最終whole-branch review(opus)→
    finishing-a-development-branch → merge
  - session 34 は determinism chain・AD-30 chain 両方とも subagent-driven-development で
    完遂(worktree分離あり)。規模・リスクに応じて使い分け継続。
