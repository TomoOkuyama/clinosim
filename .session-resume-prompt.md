# Session 33 Resume Prompt(2026-07-04+)

## ★ 追記(session 32 後半): PR #137 MERGED — chain 1b 完了、master = `d6f393445c`

**user 決定: ナラティブの実 LLM 生成は別サーバで実行。コードは完備済み。**
- `regenerate-goldens --provider/--llm-config/--model-tag` + `<profile>.llm-<tag>.golden.json` + regression llm-mock leg(12 tests、6 mock goldens 同梱)
- `semantic_check.py`(5 軸)+ `<profile>.llm-expectations.yaml` ×6 + **`check-narratives` CLI**(exit 0/1、実 LLM の byte-diff 代替 gate)
- `narrate --patient-filter <regex>`(反復チューニング用。partial version guard: set-current default off + 既存 version 書込みは `--merge-into-version` opt-in + manifest `partial` flag)
- vitals placeholders 実導出({sbp}/{hr}/{temp}/{spo2}/{rr}/{dbp})
- **リモート実行手順書: `docs/design-notes/2026-07-03-remote-llm-narrative-workflow.md`**(env 構築→narrate→check-narratives→set-current/export-fhir→LLM golden 化、copy-paste 検証済)
- 検証: unit 1022 / integration 264 / e2e 35 / regression 12 PASS

**Session 33 の選択肢**: (A) リモートサーバで実 LLM 実行(手順書に従う。user 主導 or 支援)、(B) chain 2 = 厚労省 4 帳票(入院診療計画書 18776-5 / 看護必要度 D / 栄養 / リハ、QuestionnaireResponse active 化)、(C) determinism chain。TODO.md § "β-JP-1 chain 1b adv-1 deferred (2026-07-03)" も参照。


## Session 32 で完了(cold-start context)— master = `40df7c184c`

**PR #136 MERGED — β-JP-1 chain 1a: Narrative Context CIF Wiring**
(spec: `docs/superpowers/specs/2026-07-03-beta-jp1-1a-narrative-context-wiring-design.md`)

- Stage 1: `Encounter.clinical_course_archetype` 新 field + inpatient severity/archetype
  永続化(100%)。Stage 2: `_build_context` 実 schema 配線(vital_signs / MAR /
  clinical_diagnosis / patient.allergies / disease_protocol / encounter_protocol(ed_visit)/
  narrative_spine)+ **per-stub day_index** + los_days(engine `_compute_los_days` 再利用)
- adv-1(compact 1-agent 5-lens)で 3 IMPORTANT 修正:
  - I-1 discharge_medications への MAR 全量混入(ICU drip が退院処方に)→
    `ctx.discharge_medications` 分離 + `strip_protocol_prefix` を `_shared.py` 昇格
  - I-2 placeholder 破綻文 → section 全体 fallback(pre-1a parity)
  - I-3 US 出力「Call 119」→ 911 + en YAML 119 guard test
- goldens 6 profile 再生成 ×2(AD-66 Rule 2 全 categorize、想定外 0)
- unit 967 / integration 261 / e2e 35 / regression 6 PASS

**実 LLM smoke 成功(初)**: `narrate --provider ollama --llm-config <gpt-oss:20b config>`
end-to-end 動作、llm_enabled_sections(hpi / assessment_and_plan)のみ実 LLM 置換を確認。
品質は「動くが浅い」= chain 1b のチューニング対象。
環境メモ: ローカル Ollama は **gpt-oss:20b のみ**(default config の llama3.1:8b は未 pull —
`--llm-config` で model 指定要)。AWS 認証ローカル有効。Bedrock EC2 = memory
reference_ec2_access。

## Session 33 開始点

### 状態確認(最初に必ず)

```bash
cd /Users/tokuyama/workspace/clinosim
git status && git log --oneline -3 master
# 期待: master = 40df7c184c(PR #136 merge)or 本 resume prompt commit
```

### 推奨 = β-JP-1 chain 1b: LLM golden + semantic diff + prompt 品質

1. **LLM golden 基盤**: `<profile>.llm-<model>.golden.json` 並行保存。
   mock provider = deterministic → byte-diff 可(regression suite 拡張の最短経路)。
   実 LLM = semantic diff(expected phrases / 数値 tolerance / facts_used 整合 /
   禁止 pattern(hallucination 検出))。`regenerate-goldens --provider <p>` CLI 拡張
2. **prompt 品質**: `prompts/{en,ja}/narrative_seed.yaml` 改良(現 LLM 出力は
   assessment_and_plan が浅い)。ja 書式規約(【】/■/・)。fixture 6 profiles で
   Ollama gpt-oss:20b 実行 → 目視 + semantic gate
3. **chain 1a deferred (d)**: {sbp}/{hr}/{temp} を ctx.vitals から実導出
   (placeholder section fallback の解消)
4. 余力があれば Bedrock 実行(EC2 or ローカル AWS 認証で `--provider bedrock`)

### その後の chain 順(grand-design note §4 + master plan)

chain 2(厚労省 4 帳票: 入院診療計画書 18776-5 / 看護必要度 D / 栄養 / リハ、
QuestionnaireResponse active 化)→ determinism chain(wall-clock + JP blood_type 重み)→
AD-30 chain → display-dict 移行。Bug A 系列(physical_exam_findings ja-only、US 出力に
日本語混入 — LLM smoke でも可視)は独立 data-authoring chain(TODO.md 該当 entry 参照)。

## TODO 位置

- TODO.md § "β-JP-1 chain 1a adv-1 deferred (2026-07-03)"(6 items)
- § "N-chain adv-1 deferred" / § "Common-logic unification review — deferred chains"

## 必読 memory(cold-start 順)

1. **project_session_32_end_state.md**(★★★ 本 session + 教訓 2 件)
2. project_session_31_end_state.md(N-chain / PR #132-#135)
3. feedback_scope_discipline.md / feedback_iterative_adversarial_review.md /
   feedback_pr_merge_dqr_required.md
4. project_document_density_master_plan.md + reference_ec2_access.md

## 教訓(session 32 新規)

- **golden regen は Rule 2 categorize + 臨床内容レビューの両方**(diff 形状が期待どおり
  でも「死亡患者に ICU drip 退院処方」のような臨床誤りが焼き込まれ得る — adv-1 が検出)
- wiring 変更 PR は下流 renderer の**実出力 grep** を verification に含める
  (ctx が埋まると latent 欠陥が顕在化する)

## Communication

- 日本語 default。「進めて」「おすすめで」= 継続 execution 信頼委譲
- Scope discipline(★★★): spec 確定後 scope 拡大禁止、必須のみ scope 内、他は TODO entry 化
- Chain workflow(確立済): recon → spec commit → TDD 実装 agent → 独立検証 →
  PR → compact adv review(1-agent 5-lens、実証要求)→ fix agent → 全 suite → merge
