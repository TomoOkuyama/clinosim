# Session 43 Resume Prompt(2026-07-07+、Cycle 2 CLOSED)

## ★★★ Cycle 2 CLOSED、Cycle 3 開始待ち(user 確認待ち)

**Cycle 2 結果**(session 42、`docs/audit-cycles/cycle-2.md` 参照):
- **17 issues fully RESOLVED**(C2-01/02/03/05/06/07/08/09/10/11/18/19-main/
  20-partial/27-92%/28/29/30/31/34、C2-19 kana entry は phonetic data 型変更
  が必要で partial 扱い)
- **3 issues PARTIAL**(infrastructure ready、upstream CIF populate 待ち)
  - C2-14 MR.intent monolithic still(`Order.clinical_intent` 未populate)
  - C2-15 MR no-coding 3,223 → 2,009(38% ↓)残 = procedure/device 混入
  - C2-16 MR.status no "completed"(`Order.end_datetime` 未populate)
- **2 issues NOT-A-BUG**(C2-12 Coverage.type text-only intentional、
  C2-32 severity は simulator 層、documented rationale)
- **6 issues CARRY-OVER cycle 3**:
  - CO-1 = C1-10 ImagingStudy density
  - CO-2 = C1-18 JP chronic conditions root cause
  - CO-3 = C1-09 ED procedure rules expansion
  - C2-04 ServiceRequest.code system mismatch(JLAC10 mislabeled loinc.org)
  - C2-17 Encounter.location for IMP wards
  - C2-33 Condition.stage GOLD/asthma/HTN/CCS SNOMED tail
- **新規発見**(cycle 2 fix phase 中、cycle 3 candidate):
  - CareTeam.participant.role missing 33,893(30-cap で demote)
  - MedicationRequest に procedure/device 混入(CIF classification 課題)
  - Composition.section.code 残 8% は auto-derived title
  - `_fhir_hai.py` typo `hl7-condition-verification` は cycle 2 で修正済

**Cycle 3 開始時にやること**(session 43 開始 = user 承認後):
1. Carry-over 6 件を cycle 3 opening list に追加
2. JP p=10000 再生成 → 全体レビュー + サンプリングで **30 まで積む**
   (cycle 2 partial 3 件の CIF-side upstream populate 状況確認含む)
3. 30 全て改修 → JP 再生成 → 検証 → docs 更新 → 継続確認

## Session 42 完了状態

- master HEAD: cycle 2 CLOSED commit の直後(session 43 開始時に `git log` で確認)
- Cycle 2 scratchpad: `/private/tmp/claude-.../scratchpad/cycle-2/` に jp / jp-verify /
  verify.py / verify_out.txt 保持(次サイクル時に不要なら削除可)
- **主要成果物**:
  - 新規 6 codes/data YAML(hl7-condition-clinical/ver-status/
    v3-administrativegender/subscriber-relationship/practitioner-role/v3-actreason)
  - 既存 3 YAML 拡張(snomed-ct = 185349003/11429006/394914008、loinc = 90557-9、
    hl7-admit-source = hosp、code_mapping_drug.yaml = 60+ drug bases with NOTE)
  - **canonical helper 新規**: `_coding_with_display(system_key, code, lang)` in
    `_fhir_common.py`(display=code fallback を防ぐ single edit point、
    legacy `_micro_coding` は alias で維持)
  - MedicationRequest builder: `_mr_intent_from_order` helper + `end_datetime`
    → status=completed 分岐(infrastructure only、CIF側 populate は 別 chain)
  - Patient.name: JP kanji IDE(valueCode)+ kana SYL(phonetic dict 時のみ)
  - meta.profile: Patient / Encounter / Condition(primary+chronic)/ Coverage で
    JP Core 宣言(残 20+ 未対応 = JP Core conformance pass = 別 chain)
  - Composition: `identifier` + `section.code` LOINC 30+ 種マップ

---

# Session 42 Resume Prompt(2026-07-07+、Cycle 1 CLOSED = 完了済み)

## ★★★★ サイクル型監査ワークフロー(user 明示、session 41 以降デフォルト)

**session 43 開始と同時に Cycle 3 を開いて、以下を厳守すること**:

1. **CIF/FHIR 生成**: **Cycle 2 以降は JP を先行 audit 対象**(user 明示)。
   `python -m clinosim.simulator.cli generate --population 10000 --country JP
   --seed 42 --output <path> --format fhir-r4`(サイクル内で一度)。レビューは
   **JP 医療機関適合性 + JP Core FHIR プロファイル準拠** を優先観点に、データ品質 /
   臨床整合性 / リアリティ の 3 軸で。**JP 先行 = 多言語対応構造を踏襲した上で JP を
   先にレビュー対応する**という意味であり、**JP only 化ではない**。fix は US/JP 両方に
   効く global 修正を維持(code data は en + ja 両方登録、locale gate は既存
   `is_jp`/`is_us`/`code_lookup` 経由)。
2. **全体レビュー**: 生成 NDJSON を FHIR 準拠性 / display fallback / reference integrity /
   spec 違反 datetime / silent-drop / 統計異常等の観点で監査、問題点をリスト化。
3. **ランダムサンプリング**: N 患者(5-10)をランダム抽出、その患者の全 FHIR リソースを
   1 人ずつ「データ品質 / 臨床整合性 / データのリアリティさ」観点でレビュー。
4. **反復**: 2 + 3 を **合計 30 問題点** リストアップされるまで繰り返す
   (cycle 2 以降 30-rule。cycle 1 は 20 grandfathered)。
5. **改修**: 30 問題点をすべて改修(直接 TDD 方式 or subagent 方式)。**新規追加は
   「新規追加でしか解消できない」と判断された場合のみ**(memory
   `feedback_cif_fhir_quality_focus.md`)。**★ 各 fix ごとに sibling sweep 必須**:
   1 件修正したら **他モジュールで同 class バグを都度確認 → あれば同修正**
   (memory `feedback_check_sibling_bugs_across_modules.md`)。
6. **再生成**: 改修後、同 seed / 同 population で CIF/FHIR 再生成。
7. **回収検証**: 各問題点について「回収 / 未回収」を判定・表示。
8. **★ サイクル切れ目のドキュメント更新 + 別セッション再開プロンプト記録**(必須):
   - `docs/audit-cycles/cycle-<N>.md` に検証結果(回収 / 未回収 / 新規発見)追記
   - 新知見の memory 更新、registry の新 FP entry 追加(該当時)
   - **`.session-resume-prompt.md` を「別セッション cold-start で必要十分」な状態に更新**
   - `TODO.md` の Status セクションに現サイクル進捗反映
   - commit + push 完了(cleanup 済状態)
9. **サイクル継続確認(★ 別セッション切り替え可能な状態で行う)**: docs 更新 +
   push/merge + cleanup が完了し、その状態で **別セッション cold-start でも再開可能**
   な resume prompt が用意された時点で、ユーザーに「このセッションで続けるか / 別
   セッションで続けるか」の 2 選択肢 + 回収状況 + carry-over 数を報告して確認する
   (自動継続禁止、fix + 再生成 + 回収レビュー完了後のみ)。
10. **次サイクル**: 未回収問題点を carry-over で開始 → 全体レビュー + サンプリングで
    30 まで積んで改修 → 検証 → docs 更新 → 確認…

### 進捗表示(必須、全 fix step で可視化)

`[Cycle N · n/30] <short desc>` を **すべての改修 step で表示**。
サイクル終了時に「回収 X / 未回収 Y / 新規発見 Z」を明示。

### Test 実行ポリシー(cycle 1 で確立、cycle 2 で継続確認)

- **各 fix ごと**: `pytest tests/unit -x`(~50s)
- **fix 段落ごと**: + `pytest -m regression`(+12s、golden ズレ検出)
- **cycle 再生成前 1 回**: + `pytest tests/integration -x`(+13分)+ `pytest -m e2e -x`(+3分)
- 反復中に integration/e2e を毎回回さない(subprocess 起動で遅い)。
  再生成の直前 1 回で CLI end-to-end を担保。

### 記録(必須)

- `docs/audit-cycles/README.md` = index + 手順書
- `docs/audit-cycles/cycle-<N>.md` = 各サイクルの記録(問題点 / 改修 / 回収検証)
- 各サイクル開始時に master HEAD、生成コマンド、seed、output path を記録

### 判定基準(重要度順)

1. **データ品質**(FHIR spec 準拠、display fallback ゼロ、reference integrity)
2. **臨床整合性**(値の生理学的妥当性、時系列一貫性、疾患-検査-治療の因果)
3. **データのリアリティさ**(統計分布、稀な事象の発生率、臨床現場との一致度)

詳細: memory `feedback_audit_cycle_workflow.md`(★★★★ session 41 以降必読)。

## ★★★ 作業方針(user 明示、session 40 wrap-up、session 41 以降デフォルト)

**CIF と FHIR の品質・整合性チェックとデバッグ、既存コードの修正を中心** に据える。
**新規追加は「FHIR 整合性の矛盾や異常を、新規追加をしなければ解消できない場合」に
限定** — 追加以外の解決策(既存コード修正 / データ triage / 既存 helper 経由化 /
検証 test 拡張 / YAML 修正等)で解決できるなら必ずそれを優先。

Cycle 2 実績:
- **6 codes/data YAML 追加(canonical 標準用語で不可欠、代替 nil)**
- **helper _coding_with_display 新規(既存 _micro_coding をアップグレード)**
- 既存 builder 修正で解消: 22 issue

「新規追加」= 権威 code system の未登録 code の登録が中心(HL7 THO / SNOMED CT /
LOINC / YJ code)。fabrication しない、authoritative source 明記が必須(session 42
確認: YJ code 部分に "representative product codes; verification required" NOTE 挿入)。

## ★★★ セッション開始直後、必ず実行すること

```bash
cd /Users/tokuyama/workspace/clinosim
git branch --show-current
git log --oneline -1 master
git log --oneline -1 origin/master
git status --short
git worktree list
```

現在ブランチが `master` でなければ、まず `git switch master` してから状態確認・作業を開始
すること。前セッション(42)は `master` 上で終了しており、`origin/master` と完全同期済み。

**master HEAD は session 42 終了時点で cycle 2 CLOSE commit の直後**。
これより古い場合は fetch/pull を検討(ただしユーザー明示指示なしに push/pull はしない)。

## ★★★ 作業再開前に必ず読むこと(プロジェクトコンセプト・設計の把握)

1. `docs/design-guides/project-concept-and-design.md` — プロジェクトコンセプト
2. `docs/design-guides/implementation-rules.md` — 実装不変則
3. `MODULES.md` — 30モジュール一覧
4. `CLAUDE.md` — コード規約
5. `docs/audit-cycles/cycle-2.md` — 直前サイクル完全記録(30 fixes 詳細)

## Communication

- 日本語 default。「進めて」「おすすめで」= 継続 execution 信頼委譲
- Cycle 中は 30 issues 全て改修 → 再生成 → 検証 → docs 更新 まで完遂してから
  ユーザー確認(cycle 2 実績:22 fix + 3 partial + 2 not-a-bug + 6 carry-over
  = 30 全カテゴライズを 1 セッションで完遂)
- Chain workflow(3 バリエーション、session 42 も直接 TDD 方式で完遂):
  - **5-lens adversarial review 方式**: recon → spec → TDD 実装 → 独立検証 → …
  - **subagent-driven-development 方式**: recon → spec → plan → 4 task 毎に
    実装 subagent + reviewer subagent の 2 段ゲート → …
  - **直接軽量 TDD 方式**(session 37/39/40/**42** の全体がこの方式で完遂した実例)
