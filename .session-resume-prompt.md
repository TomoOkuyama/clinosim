# clinosim Session 25 Resume Prompt

clinosim プロジェクトの作業再開です。**FHIR 生成の修正・拡張に focus**(user 指示 2026-06-30)。

## ★ Project purpose

clinosim は **EHR/EMR sample data generator** = 医療現場で生成されるデータを利活用するシステムやソフトウェア(EHR vendor / CDSS / NLP / revenue cycle / patient portal / audit compliance / imaging vendor / pharmacy)を評価・検証するための大規模データセット生成プロジェクト。詳細 = memory [[project_ehr_sample_dataset_roadmap]]。

## ★ 状態確認(セッション 24 末、2026-06-30)

- 作業ディレクトリ: `/Users/tokuyama/workspace/clinosim`
- master HEAD = `ef84b7053f`(PR #126 = PR1 ServiceRequest for Lab Orders merged 2026-06-29、open PR ゼロ)
- branch: `master`(local sync 済、`feature/pr1-servicerequest-lab` 削除済)
- テスト: 1014 unit + 954 integration + 39 e2e PASS、determinism PASS、ruff + mypy clean
- working tree に無害な scratch のみ(`.session-resume-prompt.md` / `scratchpad/*`)= production 状態

## ★ セッション 24 完了サマリー

### PR1 ServiceRequest for Lab Orders = Tier 1 #1 完了

memory [[project_ehr_sample_dataset_roadmap]] の **Tier 1 First PR** が master 着地:
- Panel-aware grouping(CBC/BMP/LFT/ABG/Lipid/Coag/UA = 1 SR per panel instance、stand-alone は 1:1)
- JP Core PLAC identifier(HL7 v2-0203)+ dual category coding(SNOMED 108252007 + v2-0074 LAB)
- `Observation.basedOn` + `DiagnosticReport.basedOn` linkage
- AD-60 audit plug-in `order_service_request` + 7 equality_checks lift_firing_proof + clinical-axis basedOn coverage gate
- AD-61 ADR(Lab ServiceRequest emission, panel-aware grouping)
- Production cohort 検証: US p=10,000 + JP p=5,000 audit 全 PASS、silent_no_op 7/7、0 dangling refs

### Adversarial chain 8 例目 converged

- 15 implementation tasks + 7 task-level fix passes
- Stage 1 final whole-branch(opus、single reviewer): 1 Important fixed
- Stage 2 **5-lens parallel fan-out**(★ 新 pattern、stage 2 default 化): 22 findings(1 Critical + 6 Important + 15 Minor)、全 closed
- Stage 3 re-verify: 0 Critical + 0 Important + 3 Minor → fix → converged

### 2 つの PR-90 class silent-no-op を検出+修正

1. **Production dict CIF crash**: `_bb_service_requests` が dataclass assume、production は dict → AttributeError 直接 verify。fix = `_o()` dual-access wrapper + subprocess full-pipeline integration smoke test
2. **stand-alone SR.code 不正 LOINC**: 94.7% SR が `system: loinc.org + code: "WBC"`(内部名)= FHIR-invalid。Observation builder と非対称。fix = `_build_standalone_sr` で同 code_map 適用

### 新 design artifact + memory + CLAUDE.md rules

- `docs/design-guides/fhir-data-generation-logic.md`(Layer 4 FHIR builder 設計、新規開発者 onboarding、★ FHIR-focused 作業の起点 doc)
- memory [[feedback_recommendation_evaluation_axes]]: 6 軸評価(データ品質 / 臨床整合 / FHIR-JP Core 準拠 / メンテ性 / モジュール責任分解 / EHR-EMR sample dataset goal)
- memory [[feedback_unify_data_logic]]: SSOT 原則(同 YAML/constant/algorithm が 2+ consumers なら canonical single source)
- CLAUDE.md DRY rules 2 件追加: `classify_lab_specs` helper + `_o()` dual-access(`scenario_flags_from_protocol` / `medication_flags_from_context` sibling pattern)
- memory [[feedback_iterative_adversarial_review]] に 5-lens fan-out pattern 追記

## ★ このセッションの focus = FHIR 生成 修正 + 拡張

user 指示(2026-06-30):**FHIR 生成の修正・拡張を中心に進める**。

### 候補(おすすめ順、6 軸評価で 1 つ選んで提示推奨)

#### ★ (A) Tier 1 #2 = Imaging metadata-only chain(★ 最有力、roadmap 順)

memory [[project_ehr_sample_dataset_roadmap]] Tier 1 #2。PR1 の panel-aware pattern + `_o()` dual-access + 5-lens adversarial fan-out template を流用。

clinosim は **text data + metadata + WADO-RS placeholder のみ生成**、DICOM pixel data は外部画像生成 AI に委譲。

含むもの:
- `ImagingStudy` resource(modality / body site / series 数 / instance 数 / accession # / 撮影日時)
- `DiagnosticReport`(radiology) with narrative findings + impression(template-based、後で LLM 化)
- `ServiceRequest`(imaging) = PR1 の SR builder を imaging category(SNOMED 363679005 "Imaging procedure" + v2-0074 "RAD")に拡張(★ PR1 で `_fhir_service_request.py` を polymorphic category 設計済)
- `Endpoint` resource = WADO-RS URL placeholder(`https://wado.clinosim.example/dicomweb/studies/{study_uid}`)
- `urn:dicom:uid` system identifier for StudyInstanceUID

含まないもの:実 DICOM ファイル(pixel data) / DICOMweb endpoint 実装 / DICOM SR

評価可能 system(metadata only でも cover): NLP/IE(radiology report)、CDSS(imaging order workflow)、Revenue cycle(imaging CPT/HCPCS/JP K-codes)、Quality reporting(turnaround time)、Patient flow、EHR migration、imaging vendor。

将来の画像生成 AI 統合点: `Endpoint.address` を実 PACS / DICOMweb サーバ URL に差し替え、`ImagingStudy.identifier[].system = "urn:dicom:uid"` で StudyInstanceUID 一致 lookup。

推定: 4-5 PR(skeleton + types / SR(imaging) builder 拡張 / ImagingStudy + radiology DR builders / Endpoint stub + audit / docs)+ adversarial fan-out 2-3 PR。

#### (B) `_BUNDLE_BUILDERS` dict-compat sweep(★ FHIR builder bug class 一括 fix、PR1 教訓直接適用)

PR1 Stage 2 で `test_device_fhir_output.py::test_device_extension_through_fhir_pipeline` が同 dataclass-vs-dict bug pattern を持つことが発見済(baseline 失敗)。全 builder を `_o()` dual-access pattern に統一する大規模 sweep。

対象 builder(PR1 で touched 済以外):
- `_bb_microbiology` / `_bb_vitals` / `_bb_medication_admins` / `_bb_procedures` / `_build_nursing_observations` / `_build_immunizations` / `_build_family_history` / `_build_code_status` / `_build_smoking_status` / `_build_alcohol_use` / `_build_care_level` / `_build_device` / `_build_device_use` / `_build_hai_conditions`

Adv-1 silent-no-op review で「PASS」だった builder も含めて再点検 = silent-no-op deep audit chain。一括 sweep + `_o()` の `_shared.py` 昇格(下記 (E))もセットで進めるのが clean。

推定: 3-5 PR(builder ごと小 PR、`_o()` promote + 全 builder migration、test 補強、audit run)。

#### (C) M-6 disease YAML `code_loinc:` backfill(FHIR data quality、JP cohort 改善)

PR1 Task 10 audit で発見: JP p=5k cohort で 105 SR(0.25%)が EN-fallback display。原因 = 約 30 disease YAML の lab エントリで `code_loinc:` field 未設定 → `Order.order_code` が内部 test name("CRP", "WBC")= JP `code_lookup("loinc", "WBC", "ja")` → fallback en。PR1 fix(`load_code_mapping`)で 部分緩和済、根本 fix は YAML backfill。

LOINC 認証 source = NLM clinicaltables.nlm.nih.gov API + JCCLS-JSLM v137 + jpfhir.jp(memory [[reference_jlac10_source]])。

推定: 30 YAML × 5 分照合 = ~3 時間、1 PR + 検証。

#### (D) M-7 snapshot PLACED-with-result bug fix(FHIR Observation/SR semantics)

PR1 Stage 2 で発見 pre-existing bug: 一部 stand-alone Order が snapshot boundary で PLACED status のまま result Observation を持つ = AD-32 snapshot invariant 違反。`test_snapshot_placed_orders_have_no_observation` が `xfail(strict=False)` でガード中。

fix path: `clinosim/modules/inpatient.py`(snapshot truncation logic)で order status を result emission と一貫させる。

推定: 1 PR、簡易 fix + xfail 削除。

#### (E) `_code_in_data` + `_o` public API promote(FHIR helper unification)

PR1 で発見: `_code_in_data` LOINC-existence helper が 3 箇所重複、`_o()` dual-access wrapper が 3 builder で同型重複。両方とも 3rd-consumer threshold met = 公開 API promote 推奨。

- `_code_in_data` → `clinosim/codes/loader.py:code_exists(system, code)` (public API)
- `_o` → `clinosim/modules/_shared.py:o(obj, name, default)` (既存 `get_attr_or_key` の thin wrapper)

推定: 1 PR、3 consumer migration + 重複削除。**(B) と組み合わせると効率良い**(`_o()` promote → 全 builder migration を 1 sweep で完了)。

## ★ 流儀リマインダ(セッション 24 確立 + 既存)

### 6 軸評価で推奨提示(memory [[feedback_recommendation_evaluation_axes]])

データ品質 / 臨床整合性 / FHIR-JP Core 準拠 / メンテナンス性 / モジュール責任分解 / EHR-EMR sample dataset goal

選択肢の比較 / 推奨提示時、必ず 6 軸表で評価。

### データ参照 / 生成ロジックは canonical single source(memory [[feedback_unify_data_logic]])

新 YAML / constant / algorithm が 2+ consumers なら必ず 1 canonical site、他は import 経由。writer↔reader shared = PR-90 silent-no-op defense。

### Workflow

- master から branch 必須、commit 末尾に `Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>` + `Claude-Session: <url>` trailers
- PR は `gh pr merge N --merge --delete-branch` → master 同期
- brainstorming → spec(`docs/superpowers/specs/`)→ writing-plans → TDD(subagent-driven-development)
- 既存コードは所与でなく 6 軸で見直し改善提案(memory [[feedback_propose_improvements_to_existing]])
- **PR/Merge 前必須**(memory [[feedback_pr_merge_dqr_required]]):audit run + DQR doc + docs sync + adversarial fan-out review
- **post-merge adversarial fan-out 必須**(memory [[feedback_iterative_adversarial_review]] = 8 例の安定 pattern)= 5-lens parallel:
  1. silent-no-op deep dive(builder dict/dataclass、`_o()` 適用漏れ、audit gate 自己沈黙、loader 欠陥)
  2. データ統一 compliance(YAML 単一 loader、canonical constant single-site、helper 重複)
  3. FHIR R4 / US Core / JP Core 厳密性(必須 field、status enum、識別子 system URI、多言語 coding)
  4. AD-16 determinism + scale invariants(sorted iteration、O(N²) ループ、入力 order independence、production cohort sanity)
  5. spec compliance + memory + CLAUDE.md adherence(各仕様 section vs 実装、TODO.md formal entry、docs sync)
- **converged で停止**(Critical/Important 0 + finding converging + 残 cosmetic only + 次段 expected size tiny)

### Pre-merge gate(セッション22 教訓)

- 新 validator / validation 拡張時は `pytest tests/unit tests/integration -m "unit or integration"` の **full sweep** を pre-merge で必ず実行(feature-specific test files のみは不十分、unrelated test の monkeypatch fixture が新 validator に hit する古典盲点)

### PR-90 class silent-no-op 防御(memory [[feedback_xhigh_review_lessons]] + セッション 24 拡張)

- **test fixture は dict + dataclass 両 path exercise**(production CIF は `json.load()` → dict、test は dataclass)
- **subprocess full-pipeline smoke test**(`json.dump → json.load → builder` の最終形を再現)
- **builder pair の reference flow を verbatim 同期**(Observation と SR で code_map / lookup 経路を非対称にしない)
- **canonical constant grep**(`grep -rn "literal_value"` を canonical 定義 site 以外で見つけたら全部 import 経由に refactor)
- **audit gate / test 内の literal も対象**(memory drift で test が false PASS、gate が silent)

### TODO.md formal entry 必須(セッション 23 教訓)

各 chain CLOSED 時に全 out-of-scope 項目を TODO.md に形式エントリ(rationale + 推奨 approach + AD-16 等 guardrail 含む)。「中途半端な状態が残らないように」最重視。

### コード値権威照合(必須)

- NLM(ICD-10-CM、LOINC、CVX)/ WHO ICD-10 / CDC(CVX)/ MHLW(YJ/K codes)/ Regenstrief(LOINC alt)/ JCCLS-JSLM v137(JLAC10)/ tx.fhir.org(SNOMED CT)/ NLM RxNav(RxNorm)
- 不確実なコードは `# TODO: verify` で残す
- 既存 codes/data yaml への追加は dedup チェック必須(`test_codes_integrity.py` で last-wins 検出)

### 確立 patterns(セッション 24 で形成 / 強化されたもの)

- **5-lens parallel adversarial fan-out**(stage 2 default、8 例目 chain converged)
- **`_o(order, name, default)` dual-access wrapper**(dict + dataclass production-safe)
- **subprocess full-pipeline smoke test**(production json.load path verify)
- **builder pair symmetry**(Observation vs ServiceRequest code_map flow 同期)
- **canonical constant writer↔reader shared**(grep-based audit、literal を import 経由に統一)
- **`OBS_ID_FORMAT` + `lab_obs_id` + `parse_lab_obs_id` public helpers**(format drift defense)
- **6-layer per-validator pattern + Layer 6b cross-validation against sibling YAML**(panel components ↔ lab_panels.yaml drift defense)
- **6-axis evaluation framework**(brainstorming + design + improvement proposal)

## ★ 主要 docs(初見 / 再開時の navigation)

- `README.md`(EN) / `README.ja.md`(JP) — user-facing overview
- `MODULES.md` — 25 module + audit framework 俯瞰
- `SCENARIO_FLAGS.md` — 全 scenario / medication flag 中央 doc
- `DESIGN.md` — ADRs(AD-61 = PR1 最新)
- `docs/CONTRIBUTING-modules.md` — CIF 生成 layer 設計 + canonical patterns + PR 検証ガイド + Cross-module URI 規約
- ★ `docs/design-guides/fhir-data-generation-logic.md` — Layer 4 FHIR builder 設計、新規開発者 onboarding(★ セッション 24 新設、FHIR-focused 作業の起点)
- `.github/TEMPLATE_MODULE_README.md` — 新モジュール README boilerplate
- `docs/reviews/2026-06-29-pr1-servicerequest-lab-dqr.md` — PR1 production DQR(参考、次 DQR の雛形)
- `docs/superpowers/specs/2026-06-29-pr1-servicerequest-lab-design.md` — PR1 spec(参考、次 PR の spec 雛形)
- 各 `clinosim/modules/<name>/README.md` — 全 25 module
- `clinosim/audit/` package + 各 module `audit.py`(hai / antibiotic / order)— AD-60 audit framework + plug-in patterns

## やってほしいこと(明確な手順)

1. memory(特に [[project_ehr_enrichment]] **セッション 24 entry**(末尾)+ [[project_ehr_sample_dataset_roadmap]] Tier 1 roadmap + [[feedback_iterative_adversarial_review]] + [[feedback_recommendation_evaluation_axes]] + [[feedback_unify_data_logic]] + [[feedback_xhigh_review_lessons]] + [[feedback_pr_merge_dqr_required]] + [[feedback_clinosim_workflow]] + [[feedback_verify_before_asserting]])+ 主要 docs(★ 新 `docs/design-guides/fhir-data-generation-logic.md`)を確認

2. **FHIR 生成 focus** 候補(A-E)から **6 軸評価で 1 つ推奨提示** → user 承認後:
   - brainstorming → spec → plan → TDD subagent-driven → audit run + DQR → docs sync → PR → 5-lens adversarial fan-out → fix → converged
   - 「中途半端な状態が残らないように」最重視
   - 全 out-of-scope 項目は TODO.md formal entry 化

3. **おすすめ進行** = **(A) Tier 1 #2 Imaging metadata-only chain**(roadmap 順、PR1 の adversarial chain pattern 流用、最大 unlock 価値、後続 Tier 1 が依存)

「**おすすめで進めて**」で OK = (A) brainstorming から開始

## コミュニケーション

すべての user-facing communication は **日本語で**(CLAUDE.md グローバル指示)。
