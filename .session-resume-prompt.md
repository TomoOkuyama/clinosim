# Session 32 Resume Prompt(2026-07-03+)

## Session 31 で完了(cold-start context)

- **★ PR #133 opened** — common-logic unification refactor + logic design guide refine
  - PR URL:https://github.com/TomoOkuyama/clinosim/pull/133
  - branch:`refactor/common-logic-unification`(2 commits:code `1816db3591` + docs `41f174c930`)
  - 4 観点並列監査(loader / code-mapping+i18n / generation+narrative IF / docs)の
    所見から **byte-identical R1-R7** を実装:
    - R1 `is_jp()` / `resolve_lang()`(`modules/_shared.py`)— 5 idiom 混在解消 +
      `_fhir_common.py` の `country != "US"` → ja fallback バグ修正
    - R2 `codes.system_key_for(kind, country)` — 国→コード系選択の単一 source(9 サイト置換)
    - R3 `_SYSTEM_URIS` 3 key 追加 + 7 literal → `get_system_uri()`
    - R4 重複 loader 4 組統合(chronic_medications / hai_organisms / antibiotic LOINC lookup /
      `_fhir_localization` ja tables → `locale/loader.py`)
    - R5 `@lru_cache` 4 loader(healthcare_config / hospital_operations / disease_protocol /
      encounter_condition、mutation-check 済 + shared-instance contract 文書化)
    - R6 `normalize_probabilities(fallback="raise")` 4 サイト(population age-band +
      activator insurance/race/ethnicity)。**blood_type のみ意図的 skip**(JP 重み合計
      0.9999999999999999 → YAML 修正が先、determinism chain へ)
    - R7 path 定数正規化 + `load_all_disease_protocols()` を disease module へ帰属
  - **検証**:unit 865 / integration 257 / e2e golden 35 PASS。byte-diff
    (3-stage full pipeline、US p=500 + JP p=300 + agent 側 p=1000):FHIR NDJSON +
    narrative 100% sha256 一致、CIF 差分 = wall-clock 2 フィールドのみ
    (`discharge_prescription.issue_date` / `physiological_states[].timestamp`)

- **★ グランドデザインレビュー note(必読)**:
  `docs/design-notes/2026-07-02-grand-design-review-and-roadmap.md`
  — グランドデザイン 9 要求への適合度評価 + deferred chain 優先度 + master plan 挿入順

- **ガイド refine**:CONTRIBUTING-modules.md(新 helper 必須 idiom + 「最初に読む ADR」+
  cross-link)/ fhir-data-generation-logic.md(E.10/E.11 anti-pattern 追加)/
  **新規 `docs/design-guides/README.md`**(新規 module 著者 7-step 読了パス)/
  module 数 30 統一(MODULES.md / CLAUDE.md / CONTRIBUTING)/ DESIGN.md AD-63 stage 修正 /
  TODO.md header 日付修正 + deferred chain formal entries 追加

## ★★ N-chain 発見(β-JP-1 の前提、最重要 finding)

Narrative 層に統合すべき継ぎ目が 4 つ(詳細 = grand-design note §3.1 + TODO.md N-chain):
1. **二重抽象**:正 = `NarrativePass` ABC。孤立 = `LLMNarrativeGenerator` +
   `replacement_strategy` + `NarrativeCache`(α-min-1 Task 7 遺産、どこからも呼ばれず、
   `DocumentTypeSpec.stage2_strategy` / `llm_enabled_sections` も dead)= PR-90 型 scaffold
2. **同名非互換 `LLMProvider` protocol ×2**(narrative 側 vs llm_service 側)
3. **prompt 所有権分裂**(`llm_service/prompts/` YAML 未消費、`_build_seed_prompt` inline)
4. `template_generator.py` 1446 行 Python hardcode(データドリブン違反)+
   `llm_service/__init__.py` 空(public API 規約違反)

## Session 32 開始点

### 状態確認(最初に必ず)

```bash
cd /Users/tokuyama/workspace/clinosim
git status && git log --oneline -3 master
gh pr view 132 --json state,mergeable   # α-min-2c fixture library
gh pr view 133 --json state,mergeable   # common-logic unification
```

### 推奨進行(grand-design note §4 の順)

0. **PR #132 merge**(fixture library、adv-1 5-lens or 直接)— β-JP-1 regression 基盤
1. **PR #133 merge**(本 refactor、byte-diff 検証済。#132 との重複は最小 —
   `simulator/cli.py` boolean gate 数行の rebase 程度)
2. **α-min-3:outpatient/ED POST_ENCOUNTER 配線** — 実装済 spec が production 0 件
   (OUTPATIENT_SOAP / ED_NOTE / ED_TRIAGE_NOTE)。~154k resource gap を最小 PR で閉鎖
3. **N-chain N-1〜N-3**(narrative IF 統一)
4. **β-JP-1 LLMNarrativePass**(fixture library + N-chain 完了で最短経路)
5. determinism chain(wall-clock 全除去 + JP blood_type YAML 修正 → full byte-diff 確立)
6. AD-30 chain(display-in-CIF 除去)+ display-dict → codes YAML 移行

## 必読 memory(cold-start 順)

1. **project_session_31_end_state.md**(★★★ 本 session)
2. project_session_30_end_state.md(PR #132 詳細)
3. feedback_scope_discipline.md / feedback_unify_data_logic.md /
   feedback_iterative_adversarial_review.md / feedback_recommendation_evaluation_axes.md
4. project_document_density_master_plan.md(7 phase roadmap、引き続き有効)

## 必読 docs

- `docs/design-notes/2026-07-02-grand-design-review-and-roadmap.md`(★ 本 session の集大成)
- `docs/design-guides/README.md`(新規著者読了パス)
- `TODO.md` § "Common-logic unification review — deferred chains"(全 deferred chain)

## Communication

- 日本語 default。Auto Mode 傾向:「進めて」「おすすめで」= 継続 execution 信頼委譲
- Scope discipline(★★★):spec 確定後 scope 拡大禁止、必須のみ scope 内、他は TODO entry 化
- 4 観点並列監査 → triage → byte-identical subset のみ refactor + 残りは formal TODO 化、
  という本 session のパターンは横断 refactor の再利用可能 template
