# Session 32 Resume Prompt(2026-07-03+)

## Session 31 で完了(cold-start context)— 3 PR merged、master = `fc0ed11c97`

1. **PR #132 MERGED**(α-min-2c fixture library、AD-66)
   - compact adv review(5-lens 1-agent 版)→ 0 CRITICAL / 3 IMPORTANT 検出 → 修正後 merge
   - F-1 unwired `PatientProfile.chronic_medications`/`time_range` 削除(β-JP-1 で consumption
     とセット再追加、TODO 化)/ F-2 test-disease CLI sentinel default(explicit `--seed 42` が
     profile に勝てない Bug-D 級)修正 / F-3 `pytest -m regression` 真の opt-in 化(conftest skip)

2. **PR #133 MERGED**(common-logic unification refactor + logic design guide refine)
   - 4 観点並列監査(loader / i18n / generation+narrative IF / docs)→ byte-identical R1-R7
   - `is_jp()`/`resolve_lang()`(`modules/_shared.py`)、`codes.system_key_for()`、URI 定数 3 追加、
     重複 loader 4 組統合、`@lru_cache` 4 loader、`normalize_probabilities` 4 サイト、
     `load_all_disease_protocols` → disease module 帰属
   - byte-diff(3-stage pipeline US p=500 + JP p=300): FHIR + narrative 100% sha256 一致
   - ガイド refine: **`docs/design-guides/README.md` 新設**(新規著者 7-step 読了パス)、
     CONTRIBUTING-modules.md 新 helper 必須 idiom 化 + ADR 読書リスト、E.10/E.11 anti-pattern、
     module 数 30 統一

3. **PR #134 MERGED**(α-min-3: nursing 3-shift + stale gap closure)
   - **★ 教訓: 着手前の status audit で 3 項目中 2 項目が stale と判明**
     - POST_ENCOUNTER 配線 = α-min-2 Task 14 で解決済(実測 US p=500: SOAP 1,841 / ED 210+210)
     - Composition.author = 配線済(`Practitioner/UNKNOWN` fallback 発火 0)
   - 実残 = **NURSING_SHIFT_NOTE 3 交代化**を実装: `daily_3shift` frequency +
     `SHIFT_SCHEDULE`(night 00:00 / day 08:00 / evening 16:00)+ `ClinicalDocument.shift`
     neutral key + Stage 2 で en night/day/evening・ja 深夜/日勤/準夜 解決 +
     `GENERATION_FREQUENCIES` fail-loud allowlist + audit 34→37 equality_checks +
     clinical_acceptance 0.8→2.4 notes/day
   - goldens 6 profile 再生成(AD-66): sepsis 76→148 / pneumonia 26→48 / COPD 24→44 /
     MI 12→20 / DKA 10→16 / stroke 10→16(diff = shift note +2x のみ、Rule 2 精査済)
   - unit 889 / integration 260 / regression 6 / e2e 35 PASS、audit 全 module PASS、
     production 3.0x 実証(US p=200: 750 vs 250)

## ★ グランドデザインレビュー(必読)

`docs/design-notes/2026-07-02-grand-design-review-and-roadmap.md` — グランドデザイン 9 要求への
適合度評価 + deferred chain 優先度 + master plan 挿入順。
TODO.md § "Common-logic unification review — deferred chains" に全 chain formal 化済み。

## Session 32 開始点

### 状態確認(最初に必ず)

```bash
cd /Users/tokuyama/workspace/clinosim
git status && git log --oneline -3 master
# 期待: master = fc0ed11c97(PR #134 merge)or 本 resume prompt commit
```

### 推奨 = N-chain(narrative IF 統一、β-JP-1 の直接前提、3-4 PR 規模)

grand-design note §3.1 + TODO.md N-chain entry が spec の出発点:
- **N-1 契約統一**: `NarrativeGenerator` Protocol(`types/document.py`)+
  `TemplateNarrativePass` constructor 注入(現在 `passes.py:262` で hardcode)+
  孤立 scaffold の wire-or-delete 判断(`llm_generator.py` / `replacement_strategy.py` /
  `cache.py` — `stage2_strategy`/`llm_enabled_sections` 全て unreachable = PR-90 class)+
  `llm_service/__init__.py` public API export(現在空)
- **N-2 provider 統一**: 同名非互換 `LLMProvider` protocol ×2
  (`narrative/replacement_strategy.py:28 generate(prompt)->str` vs
  `llm_service/providers/base.py:24 complete(...)->ProviderResponse`)を llm_service 側に
  一本化(thin adapter)。AD-11 を構造的保証
- **N-3 prompt 所有権**: `llm_service/prompts/{en,ja}/*.yaml`(AD-40)を唯一の置き場に、
  PromptRegistry を (doc_type, language) key で narrative 層から消費。
  `DocumentType`(9 種)vs `LLMTaskType` narrative subset の drift 統一
- **N-4(任意・段階的)**: `template_generator.py`(1446 行 Python)の section template YAML 化

その後: **β-JP-1 LLMNarrativePass**(fixture library + N-chain 完了で最短経路)→
determinism chain(wall-clock 全除去 + `locale/jp/demographics.yaml` blood_type 重み
0.9999999999999999 修正 → full byte-diff 確立)→ AD-30 chain(display-in-CIF 除去)→
display-dict → codes YAML 移行

## 必読 memory(cold-start 順)

1. **project_session_31_end_state.md**(★★★ 本 session、3 PR + N-chain 詳細)
2. project_session_30_end_state.md(fixture library 詳細)
3. feedback_scope_discipline.md / feedback_unify_data_logic.md /
   feedback_iterative_adversarial_review.md / feedback_pr_merge_dqr_required.md
4. project_document_density_master_plan.md(7 phase roadmap、引き続き有効)

## 必読 docs

- `docs/design-notes/2026-07-02-grand-design-review-and-roadmap.md`(★ session 31 集大成)
- `docs/design-guides/README.md`(新規著者読了パス)
- `TODO.md` § "Common-logic unification review — deferred chains"(N-chain ほか全 deferred)

## 教訓(session 31 新規)

- **★ stale TODO は着手前に実測 status audit**: α-min-3 の「CRITICAL 配線 gap」は 2 世代前に
  解決済だった。scratchpad の実 cohort 出力を数えるだけで 1 PR 分の無駄を回避できた
- 4 観点並列監査 → triage → byte-identical subset のみ refactor + 残り formal TODO 化 =
  横断 refactor の再利用可能 template
- compact adv review(5-lens を 1 agent に折込み)は小 scope PR(#132)に有効
  (大 chain は従来どおり 5-agent fan-out)

## Communication

- 日本語 default。Auto Mode 傾向: 「進めて」「おすすめで」= 継続 execution 信頼委譲
- Scope discipline(★★★): spec 確定後 scope 拡大禁止、必須のみ scope 内、他は TODO entry 化
