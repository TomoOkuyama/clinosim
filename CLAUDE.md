# CLAUDE.md — pointer to AGENTS.md

The canonical AI-agent instructions for this repository now live at
[`AGENTS.md`](AGENTS.md) (following the [AGENTS.md convention](https://agentmd.dev)).

Everything Claude Code needs is there — read that file, not this one. This
pointer exists only so that older Claude Code sessions (which look for
`CLAUDE.md` by name) still find their way in.

Personal / user-scope preferences (Japanese communication style, obsidian
vault workflow, etc.) that used to live here have moved to
`~/.claude/CLAUDE.md` in the maintainer's home directory — they are
maintainer preferences, not repo-level agent instructions.
